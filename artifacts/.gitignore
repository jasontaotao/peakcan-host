# Published binaries are regenerated by 'dotnet publish' — keep the
# directory under version control (the README lives here) but ignore
# the actual exe / pdb outputs.
PeakCan.Host.exe
PeakCan.Host.pdb
PeakCan.Host.Core.pdb
PeakCan.Host.Infrastructure.pdb

# SecOC M2 demo assets (generated by scripts/secoc-demo/gen_demo_assets.py;
# key material is never written to these files - MACs and keyId refs only)
!secoc-demo/
