# v1.5.1 PATCH Implementation Plan

**Date:** 2026-06-29
**Branch:** `feature/v1-5-1-patch` (cut from main @ v1.4.2 `a77191a`)
**Target version:** v1.5.1 (PATCH)
**Spec:** `docs/superpowers/specs/2026-06-29-v1-5-1-patch-design.md`
**Owner:** main agent + parallel TDD subagents
**Reviewer:** code-reviewer subagent (pre-ship)

## 概述

3 PATCH items ship sequence:

```
Task 0: Setup (worktree, baseline)              [DONE]
Task 1: Item 3 cleanup — Remove [Obsolete]      [FIRST — smallest, unblocks ref]
Task 2: Item 1 — Replay time-range filter       [TDD]
Task 3: Item 2 — Periodic DBC send              [TDD]
Task 4: Ship prep                               [PR + tag + release]
```

**Order rationale**: Item 3 是 smallest、零 risk、不依赖其他 items；先 ship 减少后续 PR 的 scope 复杂度。Item 1 + Item 2 互相独立，并行 subagents 可同时启动（但写 plan 时按串行描述，实际 execute 可 parallel）。

## Task 0: Setup (DONE)

| Sub-task | Status |
|---|---|
| Phase 2.5 actual code exploration (3 parallel Explore agents) | DONE — reports integrated into spec |
| Cut worktree `feature/v1-5-1-patch` from main `a77191a` | DONE — worktree at `D:/claude_proj2/peakcan-host-v1-5-1` |
| Write spec `docs/superpowers/specs/2026-06-29-v1-5-1-patch-design.md` | DONE |
| Write plan (this file) | IN-PROGRESS |

Baseline test count (from v1.4.2 PATCH ship): 775 + 6 SKIP + 0 fail (Core 321 + App 370 + Infra 84).

Expected test count delta: +368 LOC of tests across 6 test files (5 modify + 3 new) → approximately +30 to +40 new test cases (some tests share fixtures).

## Task 1: Item 3 — Remove stale [Obsolete] SendCount

**Estimated time**: 15-30 min.
**Files**:
- Modify `src/PeakCan.Host.App/Services/ICyclicSendService.cs` — DELETE lines 20-22 (3-line `[Obsolete]` attribute + property decl)
- Modify `src/PeakCan.Host.App/Services/CyclicSendService.cs` — DELETE lines 55-57 (3-line `[Obsolete]` attribute + property impl)
- Modify `src/PeakCan.Host.App/ViewModels/SendViewModel.cs` — DELETE `[ObservableProperty] private long _sendCount;` line 57 + DELETE `OnSendCountChanged` partial method body if any
- Verify `src/PeakCan.Host.App/Views/SendView.xaml` — grep `SendCount`; remove any残留 XAML binding (v1.5.0 已 ship SuccessCount/FailureCount, but old binding may linger in CyclicExpander at line 56-60 area)

**Pre-flight verification**:
```bash
grep -rn "SendCount" src/PeakCan.Host.App tests/
```
Expected output: 0 matches in tests/ after cleanup. If残留 in XAML, note exact binding path.

**TDD approach**: This is a deletion task — no new RED tests. Existing `CyclicSendServiceTests` + `CyclicSendServiceRaceTests` should pass unchanged (they use SuccessCount + FailureCount). SendViewModelTests pass unchanged.

**Verification**:
1. `dotnet build PeakCan.Host.slnx` → 0 errors.
2. `dotnet test --filter "FullyQualifiedName~Cyclic"` → pass.
3. `dotnet test --filter "FullyQualifiedName~SendViewModel"` → pass.
4. `grep -rn "SendCount" src/PeakCan.Host.App tests/` → 0 matches.

**Commit message**:
```
chore(cyclic): remove [Obsolete] SendCount (deadline stale 12 releases)
```

## Task 2: Item 1 — Replay time-range filter

**Estimated time**: 4-6 hours (TDD cycle).
**Strategy**: RED → GREEN → IMPROVE per layer (Core interface → Core Timeline → App VM → App XAML).

### Task 2.1: Core — IReplayService + ReplayTimeline + ReplayService

**Sub-task 2.1.a: RED tests** (ReplayTimelineTests.cs + IReplayServiceTests.cs):

Add 9 [Fact] methods to `ReplayTimelineTests.cs` (after line 376):
```csharp
[Fact]
public void OnTick_StartTimestampSet_SkipsFramesBeforeStart()
{
    // Frames at t=0, 1, 2, 3; StartTimestamp=1.5; emit expected: [2, 3]
}

[Fact]
public void OnTick_EndTimestampSet_SkipsFramesAfterEnd()
{
    // Frames at t=0, 1, 2, 3; EndTimestamp=1.5; emit expected: [0, 1]
}

[Fact]
public void OnTick_StartAndEndTimestampSet_EmitsOnlyFramesInRange()
{
    // Frames at t=0, 1, 2, 3, 4; Start=1.5 End=2.5; emit expected: [2]
}

[Fact]
public void OnTick_RangeFilter_BoundaryInclusive()
{
    // Frames at t=0, 1, 2, 3; Start=1 End=2; emit expected: [1, 2]
}

[Fact]
public void OnTick_RangeFilter_NullMeansUnbounded()
{
    // Frames at t=0, 1, 2; Start=1 End=null; emit expected: [1, 2]
    // + Frames at t=0, 1, 2; Start=null End=1; emit expected: [0, 1]
}

[Fact]
public void OnTick_RangeFilter_LoopRewindReappliesRange()
{
    // Frames at t=0, 1, 2, 3; Start=1.5; Loop=true; play until >4 ticks;
    // assert first emit after rewind is frame at t=2 (in-range), not t=0
}

[Fact]
public void OnTick_RangeFilter_EmptiesAllFrames_StillRaisesPlaybackEndedOnEof()
{
    // Frames at t=0, 1, 2; Start=5 (out of range); play; assert endedCount==1 with Error=null
}

[Fact]
public void OnTick_RangeFilter_SeekOutsideRange_EmitsNothingOnPlay()
{
    // Frames at t=0, 1, 2; Start=5; Seek(0); play; assert emitted==[]
    // + Seek(5); play; assert emitted==[]; endedCount==1
}

[Fact]
public void OnTick_RangeFilter_ChangedAtRuntime_TakesEffectImmediately()
{
    // Start with Start=null; emit all; set Start=1.5 mid-play; next emit is in-range only
}
```

Add 5 [Fact] methods to `IReplayServiceTests.cs` (after line 342):
```csharp
[Fact]
public void SetStartTimestamp_GetterReturnsWhatWasSet() { ... }

[Fact]
public void SetEndTimestamp_GetterReturnsWhatWasSet() { ... }

[Fact]
public void SetStartTimestamp_PropagatesToInternalTimeline()
{
    // Mirror SetLoop_PropagatesToInternalTimeline line 237-263 pattern with reflection
}

[Fact]
public void SetStartTimestamp_Null_MeansUnbounded() { ... }

[Fact]
public void SetStartTimestamp_PropagatesToInternalTimeline_EndTimestamp_Mirror()
{
    // Same pattern for EndTimestamp
}
```

Run tests → all RED (CS0117 / CS1061 compile errors + runtime failures).

**Sub-task 2.1.b: GREEN — Add API + enforcement**:

1. `src/PeakCan.Host.Core/Replay/IReplayService.cs`: ADD `double? StartTimestamp { get; set; }` + `double? EndTimestamp { get; set; }` properties after line 43 (after CanIdFilter), with XML doc mirroring CanIdFilter's tri-state doc.

2. `src/PeakCan.Host.Core/Replay/ReplayTimeline.cs`:
   - ADD `_startTimestamp` + `_endTimestamp` private fields alongside `_loop` (around line 23).
   - ADD `StartTimestamp` / `EndTimestamp` properties (mirror `Loop` property lines 51-55: lock-guarded get/set).
   - MODIFY `OnTick` (lines 155-213):
     - At top of locked block (after line 161), capture `var startOrMin = _startTimestamp ?? double.NegativeInfinity; var endOrMax = _endTimestamp ?? double.PositiveInfinity;`
     - MODIFY while-loop predicate (line 163) to include `&& _frames[_nextFrameIndex].Timestamp >= startOrMin && _frames[_nextFrameIndex].Timestamp <= endOrMax`.

3. `src/PeakCan.Host.Core/Replay/ReplayService.cs`: ADD `StartTimestamp` / `EndTimestamp` properties after line 51 (after CanIdFilter), proxying to `_timeline.StartTimestamp` / `_timeline.EndTimestamp` exactly like the `Loop` proxy at lines 45-49.

Run tests → GREEN.

**Sub-task 2.1.c: IMPROVE — Refactor if needed**:
- XML doc on new properties must mention: composition with CanIdFilter, re-application after Loop rewind, "takes effect on next emit", thread-safety (lock-guarded via Timeline).
- Verify no race condition in `SetFrames` (line 66-74): `_frames` swap under lock, `_startTimestamp` / `_endTimestamp` persist (NOT reset). Test confirms.

**Commit message**:
```
feat(replay): time-range filter (StartTimestamp/EndTimestamp on IReplayService)
```

### Task 2.2: App — ReplayViewModel + ReplayView.xaml

**Sub-task 2.2.a: RED tests** (ReplayViewModelTests.cs):

Add 4 [Fact] methods (after line 322):
```csharp
[Fact]
public void StartTimestamp_Set_PropagatesToService()
{
    _sut.StartTimestamp = 1.5;
    _service.Received(1).StartTimestamp = 1.5;
}

[Fact]
public void StartTimestamp_Null_ClearsToService()
{
    _sut.StartTimestamp = null;
    _service.Received(1).StartTimestamp = null;
}

[Fact]
public void StartTimestamp_GreaterThanEndTimestamp_SetsRangeFilterError()
{
    _sut.EndTimestamp = 1.0;
    _sut.StartTimestamp = 2.0;
    Assert.Equal("Start must be ≤ End", _sut.RangeFilterError);
    // _service.DidNotReceive().StartTimestamp = ...
}

[Fact]
public void OpenAsync_ClearsRangeFilter()
{
    _sut.StartTimestamp = 1.0;
    _sut.EndTimestamp = 2.0;
    await _sut.OpenAsync("test.asc"); // assume helper writes temp asc
    Assert.Null(_sut.StartTimestamp);
    Assert.Null(_sut.EndTimestamp);
    Assert.Null(_sut.RangeFilterError);
}
```

Run tests → RED.

**Sub-task 2.2.b: GREEN — Add VM properties + XAML**:

1. `src/PeakCan.Host.App/ViewModels/ReplayViewModel.cs`:
   - ADD `[ObservableProperty] private double? _startTimestamp;` (after line 86)
   - ADD `[ObservableProperty] private double? _endTimestamp;`
   - ADD `[ObservableProperty] private string? _rangeFilterError;`
   - ADD `partial void OnStartTimestampChanged(double? value)` (mirror `OnCanIdFilterTextChanged` line 147-223 for validation pattern)
   - ADD `partial void OnEndTimestampChanged(double? value)` (mirror)
   - In `OpenAsync` (line 234-257), AFTER successful `LoadAsync`, add:
     ```csharp
     StartTimestamp = null;
     EndTimestamp = null;
     RangeFilterError = null;
     ```

2. `src/PeakCan.Host.App/Views/ReplayView.xaml`:
   - MODIFY `Grid.RowDefinitions` (lines 31-37) to add a 6th `Auto` row.
   - ADD new Row 5 (after current Row 4 at line 103-106) with:
     ```xml
     <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,4,0,0">
         <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,4,0" />
         <TextBox Text="{Binding StartTimestamp, StringFormat=F3, TargetNullValue='',
                      UpdateSourceTrigger=PropertyChanged}" Width="80" />
         <TextBlock Text="To:" VerticalAlignment="Center" Margin="12,0,4,0" />
         <TextBox Text="{Binding EndTimestamp, StringFormat=F3, TargetNullValue='',
                      UpdateSourceTrigger=PropertyChanged}" Width="80" />
         <TextBlock Text="{Binding RangeFilterError}" Foreground="Red"
                    VerticalAlignment="Center" Margin="12,0,0,0" />
     </StackPanel>
     ```

Run tests → GREEN.

**Sub-task 2.2.c: IMPROVE**:
- Verify XAML `StringFormat=F3` for 3-decimal display matches `TotalDuration` precision (Phase 2.5 confirmed `Timestamp` is F6 float; UI display F3 is reasonable rounding).
- Verify `UpdateSourceTrigger=PropertyChanged` for live validation (mirror `CanIdFilterText` line 92 pattern).

**Commit message**:
```
feat(app): Replay time-range UI (StartTimestamp/EndTimestamp TextBoxes + validation)
```

### Task 2.3: Pre-Item-1 verification

- Full test suite: `dotnet test` → +18 tests passing (Core +13 from 2.1.a + App +5 from 2.2.a — slightly more if boundary tests need sub-cases).
- Manual smoke: open ReplayView, type range, play, verify only in-range frames emitted.

## Task 3: Item 2 — Periodic DBC send

**Estimated time**: 6-8 hours (new service + DI + VM + XAML + tests).
**Strategy**: Service-first → DI → VM → XAML → VM tests → race regression.

### Task 3.1: Core/App — New `ICyclicDbcSendService` + `CyclicDbcSendService`

**Sub-task 3.1.a: RED tests** (CyclicDbcSendServiceTests.cs NEW + CyclicDbcSendServiceRaceTests.cs NEW):

Create `tests/PeakCan.Host.App.Tests/Services/CyclicDbcSendServiceTests.cs` (mirror `CyclicSendServiceTests.cs:128`):
```csharp
// Header comment block: "Known transient-flaky (memory v1.2.12 lesson 4) for race tests; this file is happy-path only"
// Helpers: FakeSendService (mirror CountingSendService from CyclicSendServiceRaceTests.cs:33-46)
[Fact] public void IsRunning_False_By_Default() { ... }
[Fact] public void Start_Sets_IsRunning_True() { ... }
[Fact] public void Stop_Sets_IsRunning_False() { ... }
[Fact] public void Stop_Is_Idempotent() { ... }
[Fact] public void Start_EncodesDbcMessage_Periodically() { ... }
[Fact] public void Start_EncodesWithUpdatedSignalValues_OnEachTick() { ... }
[Fact] public void Start_Stops_Previous_Cyclic_DbcSend() { ... }
[Fact] public void Start_EncodeFailure_IncrementsFailureCount() { ... }
[Fact] public void Start_MessageChangedMidRun_StopsService() { ... }
```

Create `tests/PeakCan.Host.App.Tests/Services/CyclicDbcSendServiceRaceTests.cs` (mirror `CyclicSendServiceRaceTests.cs:153`):
```csharp
// Header comment: "CyclicDbcSendServiceRaceTests known transient-flaky (memory v1.2.12 lesson 4); CI re-run 3×"
// Helpers: CountingSendService : SendService (mirror CyclicSendServiceRaceTests.cs:33-46)
[Fact] public void OnTimerTick_After_Stop_Does_Not_Send() { ... }
[Fact] public void OnTimerTick_Generation_Mismatch_Does_Not_Send() { ... }
[Fact] public void Encode_Failure_Increments_FailureCount_Not_SuccessCount() { ... }
[Fact] public void Send_Success_Increments_SuccessCount_Not_FailureCount() { ... }
[Fact] public void SuccessCount_And_FailureCount_Exposed_Via_Properties() { ... }
```

Run tests → all RED (compile errors: type doesn't exist).

**Sub-task 3.1.b: GREEN — Create service**:

1. Create `src/PeakCan.Host.App/Services/ICyclicDbcSendService.cs` (NEW, ~30 LOC):
```csharp
namespace PeakCan.Host.App.Services;

public interface ICyclicDbcSendService
{
    bool IsRunning { get; }
    long SuccessCount { get; }
    long FailureCount { get; }

    void Start(
        Func<(Message message, IReadOnlyDictionary<string, double> values)> frameProvider,
        TimeSpan interval);

    void Stop();
}
```

2. Create `src/PeakCan.Host.App/Services/CyclicDbcSendService.cs` (NEW, ~150 LOC):
- `public sealed partial class CyclicDbcSendService : ICyclicDbcSendService, IDisposable`
- Ctor: `(DbcEncodeService encoder, SendService sendService, ILogger<CyclicDbcSendService> logger)`
- Fields: `_encoder`, `_sendService`, `_logger`, `_timer`, `_frameProvider`, `_interval`, `_generation`, `_sendSuccessCount`, `_sendFailureCount`, `_isRunning`, `_capturedMessageId`
- Methods: `Start`, `Stop`, `StopInner`, `OnTimerTick` (async void, try/catch DbcSignalEncodeException + SendAsync Result), `Dispose`
- 4 `[LoggerMessage]` partial methods (mirror CyclicSendService.cs:179-189 pattern)
- Per Decision 9 refinement: capture `_capturedMessageId` from first frameProvider invocation; each tick, re-invoke frameProvider and compare message.Id — if differs → stop + log warning + FailureCount++ (one-time leak)
- Per Decision 10: encode exception caught → FailureCount++, log every 100th
- Stale-timer drop pattern (mirror CyclicSendService.cs:142)

Run tests → GREEN.

**Sub-task 3.1.c: IMPROVE — XML doc + thread-safety review**:
- All public methods have XML doc.
- Verify `_lock(this)` discipline in `Start`/`Stop`/`OnTimerTick` matches `CyclicSendService` pattern.
- CA2012 analyzer warning addressed if any fire-and-forget appears (prefer `GetAwaiter().GetResult()` in lock, mirror `ReplayService.cs:131`).

**Commit message**:
```
feat(dbc): Periodic DBC send service (CyclicDbcSendService)
```

### Task 3.2: DI registration

Modify `src/PeakCan.Host.App/Composition/AppHostBuilder.cs`:
- After `DbcEncodeService` registration block (around line 200), ADD:
  ```csharp
  builder.Services.AddSingleton<CyclicDbcSendService>();
  builder.Services.AddSingleton<ICyclicDbcSendService>(sp => sp.GetRequiredService<CyclicDbcSendService>());
  ```

Verify: `dotnet build` → 0 errors.

### Task 3.3: App — DbcSendViewModel

**Sub-task 3.3.a: RED tests** (DbcSendViewModelCyclicTests.cs NEW):

Create `tests/PeakCan.Host.App.Tests/ViewModels/DbcSendViewModelCyclicTests.cs` (mirror `DbcSendViewModelTests.cs:180`):
```csharp
[Fact] public void StartDbcCyclic_WithoutSelectedMessage_DoesNothing() { ... }
[Fact] public void StartDbcCyclic_WithSelectedMessage_CallsService_AndSetsIsRunningTrue() { ... }
[Fact] public void StopDbcCyclic_CallsService_AndSetsIsRunningFalse() { ... }
[Fact] public void StartDbcCyclic_WithInvalidIntervalText_DoesNothing() { ... }
[Fact] public void StartDbcCyclic_BuildsCurrentSignalValuesFromSignalRows() { ... }
```

Run tests → RED.

**Sub-task 3.3.b: GREEN — Add VM commands + state**:

1. `src/PeakCan.Host.App/ViewModels/DbcSendViewModel.cs`:
   - Modify ctor (line 49) to accept `ICyclicDbcSendService cyclicDbc` parameter (4th ctor param after DbcEncodeService + SendService + DbcService)
   - ADD private field `private readonly ICyclicDbcSendService _cyclicDbc;`
   - ADD `[ObservableProperty] private string? _dbcCyclicIntervalText = "100";`
   - ADD `[ObservableProperty] private bool _isDbcCyclicRunning;`
   - ADD `[ObservableProperty] private long _dbcCyclicSuccessCount;`
   - ADD `[ObservableProperty] private long _dbcCyclicFailureCount;`
   - ADD `private DispatcherTimer? _cyclicPollTimer;` (mirror `SendViewModel.cs:118-128`)
   - ADD `partial void OnIsDbcCyclicRunningChanged(bool value)` to start/stop `_cyclicPollTimer`
   - ADD `partial void OnDbcCyclicSuccessCountChanged(long value)` and `OnDbcCyclicFailureCountChanged(long value)` (or poll-driven update)
   - ADD `[RelayCommand(CanExecute = nameof(CanStartDbcCyclic))] private void StartDbcCyclic()` (mirror `SendViewModel.cs:293 StartCyclic` pattern)
   - ADD `[RelayCommand(CanExecute = nameof(CanStopDbcCyclic))] private void StopDbcCyclic()`
   - ADD `private bool CanStartDbcCyclic() => SelectedDbcMessage is not null && !IsDbcCyclicRunning && TimeSpan.TryParse(DbcCyclicIntervalText, out _);`
   - ADD `private bool CanStopDbcCyclic() => IsDbcCyclicRunning;`
   - ADD `private Dictionary<string, double> BuildCurrentSignalValues()` (mirror `SendAsync` line 137-141)
   - Modify `OnSelectedDbcMessageChanged` (line 110-118): if `IsDbcCyclicRunning` → call `StopDbcCyclic()` first (user changing message while periodic send active is user-hostile; auto-stop)
   - In ctor, initialize `_cyclicPollTimer = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(200) }; _cyclicPollTimer.Tick += (s, e) => { DbcCyclicSuccessCount = _cyclicDbc.SuccessCount; DbcCyclicFailureCount = _cyclicDbc.FailureCount; };`

2. `src/PeakCan.Host.App/Composition/AppHostBuilder.cs`: update DI registration to inject `ICyclicDbcSendService` into `DbcSendViewModel` ctor (auto-resolved by DI).

Run tests → GREEN.

**Sub-task 3.3.c: IMPROVE**:
- Verify DispatcherTimer dispose / lifetime.
- Verify CanExecute fires on all dependencies (StartDbcCyclic needs SelectedDbcMessage + IsDbcCyclicRunning + IntervalText valid).

**Commit message**:
```
feat(app): DbcSendViewModel periodic commands + state polling
```

### Task 3.4: XAML — DbcModeExpander UI

Modify `src/PeakCan.Host.App/Views/SendView.xaml`:
- In `DbcModeExpander` StackPanel (lines 109-133), AFTER the existing one-shot Send row (line 127-131), ADD:
  ```xml
  <StackPanel Orientation="Vertical" Margin="0,8,0,0">
      <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
          <TextBlock Text="Cyclic interval (ms):" Width="140" VerticalAlignment="Center" />
          <TextBox Text="{Binding DbcCyclicIntervalText, UpdateSourceTrigger=PropertyChanged}" Width="80" />
      </StackPanel>
      <StackPanel Orientation="Horizontal">
          <Button Content="Start cyclic" Command="{Binding StartDbcCyclicCommand}" Width="100"
                  IsEnabled="{Binding IsDbcCyclicRunning, Converter={StaticResource InverseBool}}" />
          <Button Content="Stop" Command="{Binding StopDbcCyclicCommand}" Width="60"
                  IsEnabled="{Binding IsDbcCyclicRunning}" />
          <TextBlock VerticalAlignment="Center" Margin="12,0,0,0">
              <Run Text="{Binding DbcCyclicSuccessCount, Mode=OneWay}" />
              <Run Text=" ok / " />
              <Run Text="{Binding DbcCyclicFailureCount, Mode=OneWay}" />
              <Run Text=" fail" />
          </TextBlock>
      </StackPanel>
  </StackPanel>
  ```

**Commit message**:
```
feat(app): DBC mode periodic send UI (interval + Start/Stop + counters)
```

### Task 3.5: Pre-Item-2 verification

- Full test suite: `dotnet test` → +19 tests passing (App +9 happy-path + 5 race + 5 VM = 19).
- Manual smoke: load DBC, select message, set signal values, click Start cyclic, verify periodic emission via trace or counter.

## Task 4: Ship prep

### Task 4.1: Pre-ship code review

Dispatch `code-reviewer` subagent against the full diff `git diff main..HEAD`. Expect 0C/0H. Address findings.

### Task 4.2: Release notes

Author `docs/release-notes-v1.5.1.md` (~200-400 lines):
- Overview (3 PATCH items)
- Items detail (Item 1 Replay time-range; Item 2 Periodic DBC; Item 3 [Obsolete] cleanup)
- Migration notes (Item 3 SendCount removal; Item 1 + Item 2 additive only)
- Process lessons learned
- Test count delta

### Task 4.3: Commit + push + PR

```bash
cd "D:/claude_proj2/peakcan-host-v1-5-1"
git add -A
git status  # verify clean intent
git commit -m "v1.5.1 PATCH: Replay time-range + Periodic DBC + cleanup (3 items) (#PR)"
# (commit message includes the standard trailer per project convention)
git -c http.proxy="" -c https.proxy="" push -u origin feature/v1-5-1-patch
# If first push fails with proxy, retry with unset (per v1.4.0/v1.4.1/v1.4.2/v1.5.0 memory)
gh pr create --base main --title "v1.5.1 PATCH: Replay time-range + Periodic DBC + [Obsolete] cleanup" --body-file docs/release-notes-v1.5.1.md
gh pr merge --squash --delete-branch  # expect first-attempt fast-forward proxy error
git fetch origin main  # recover if needed
git -c http.proxy="" -c https.proxy="" reset --hard origin/main  # squash SHA
```

### Task 4.4: Tag + release

```bash
git tag v1.5.1
git -c http.proxy="" -c https.proxy="" push origin v1.5.1
gh release create v1.5.1 --notes-file docs/release-notes-v1.5.1.md
```

### Task 4.5: MEMORY update

Add memory file `peakcan-host-v1-5-1-shipped.md` (analog to existing `peakcan-host-v1-5-0-shipped.md`) with:
- Items (3): Item 1 time-range filter + Item 2 Periodic DBC + Item 3 [Obsolete] cleanup
- Test count delta
- Process lessons
- Ship method

Update `MEMORY.md` index entry (one-line) for v1.5.1.

**Memory consolidation note**: MEMORY.md is 40KB (limit 24.4KB per harness warning). Index entries should be condensed to one line ~200 chars; full details in topic files.

### Task 4.6: Cleanup worktree (optional)

After ship, can `git worktree remove ../peakcan-host-v1-5-1` to clean local worktree.

## Risk + Mitigation Summary

| Risk | Mitigation |
|---|---|
| Brief drift on "time-range" interpretation (4-doc ambiguity) | Spec adopts v1.5.0 design spec "start/end timestamps" reading; documented Decision 1; Phase 2.5 grep confirms 0 prior range symbols. |
| Range filter in wrong boundary (emit vs iteration) | Decision 1 explicit; RED test for "Loop+range cursor behavior" catches misplacement. |
| Range validation breaks WPF two-way binding | Decision 4 uses VM-side validation + RangeFilterError, not ArgumentException. |
| Periodic DBC service pollutes CyclicSendService race-fix invariants | Decision 7 keeps independent service; no code sharing. |
| Periodic DBC Message-changed silent degradation | Decision 9 captures + id-checks each tick; explicit stop + FailureCount++. |
| Transient-flaky race tests block CI | Decision 11 + PR description caveat; mirror CyclicSendServiceRaceTests known flake pattern. |
| [Obsolete] SendCount removal breaks compile | Pre-flight grep verification (Task 1 verification step). |
| Proxy issues during git push / gh pr merge | Mirror v1.4.2/v1.5.0 pattern: explicit `git -c http.proxy="" -c https.proxy=""` + `git fetch + reset` recovery. |
| MEMORY.md size limit (24.4KB) | Task 4.5 explicit consolidation note + topic-file split. |

## File modification summary

### New files (6)
- `src/PeakCan.Host.App/Services/ICyclicDbcSendService.cs` (~30 LOC)
- `src/PeakCan.Host.App/Services/CyclicDbcSendService.cs` (~150 LOC)
- `tests/PeakCan.Host.App.Tests/Services/CyclicDbcSendServiceTests.cs` (~120 LOC)
- `tests/PeakCan.Host.App.Tests/Services/CyclicDbcSendServiceRaceTests.cs` (~150 LOC)
- `tests/PeakCan.Host.App.Tests/ViewModels/DbcSendViewModelCyclicTests.cs` (~80 LOC)
- `docs/release-notes-v1.5.1.md` (~300 LOC)

### Modified files (10)
- `src/PeakCan.Host.App/Services/ICyclicSendService.cs` (-3 lines)
- `src/PeakCan.Host.App/Services/CyclicSendService.cs` (-3 lines)
- `src/PeakCan.Host.App/ViewModels/SendViewModel.cs` (-3 lines)
- `src/PeakCan.Host.App/Views/SendView.xaml` (+1 Row in CyclicExpander area for cleanup verification; +1 sibling StackPanel in DbcModeExpander)
- `src/PeakCan.Host.Core/Replay/IReplayService.cs` (+30 lines for 2 properties + XML doc)
- `src/PeakCan.Host.Core/Replay/ReplayTimeline.cs` (+20 lines: 2 fields + 2 properties + OnTick predicate modification)
- `src/PeakCan.Host.Core/Replay/ReplayService.cs` (+20 lines: 2 property proxies)
- `src/PeakCan.Host.App/ViewModels/ReplayViewModel.cs` (+30 lines: 2 ObservableProperty + 2 OnXxxChanged + RangeFilterError + OpenAsync clear)
- `src/PeakCan.Host.App/Views/ReplayView.xaml` (+10 lines: Row 5 with 2 TextBox + error TextBlock; +1 RowDefinition)
- `src/PeakCan.Host.App/ViewModels/DbcSendViewModel.cs` (+50 lines: 4 ObservableProperty + 2 RelayCommand + 2 CanXxx + BuildCurrentSignalValues + DispatcherTimer)
- `src/PeakCan.Host.App/Composition/AppHostBuilder.cs` (+2 lines for DI registration)
- `tests/PeakCan.Host.Core.Tests/Replay/IReplayServiceTests.cs` (+80 LOC: 5 new tests)
- `tests/PeakCan.Host.Core.Tests/Replay/ReplayTimelineTests.cs` (+180 LOC: 9 new tests)
- `tests/PeakCan.Host.App.Tests/ViewModels/ReplayViewModelTests.cs` (+60 LOC: 4 new tests)

Total: ~16 files modified/created, ~1100 LOC net change (half tests, half src + XAML + docs).