# v1.6.5 PATCH Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v1.6.0 MINOR item 2 (CanApi rate limit → token-bucket send-rate-limit decorator) as v1.6.5 PATCH. 1 item, ~+7 net tests, opt-in via `appsettings.json`, 5 UI callers wrapped, Replay + IsoTp exempt.

**Architecture:** Decorator pattern — `RateLimitedSendService : SendService` registered as `SendService` type. Internal raw `CoreSendService : SendService` registered separately for Replay + IsoTp exempt. UI callers resolve `SendService` (C# 多态 → get decorator). Replay + IsoTp resolve `CoreSendService` (raw). Token bucket state (`_tokens: double` + `_lastRefillTimestamp: long`) under `lock(this)`. Reject returns `Result.Fail(ErrorCode.HardwareBusy, "rate limit ...")`.

**Tech Stack:** C# .NET 10, xUnit + FluentAssertions, `System.Diagnostics.Stopwatch` (.NET BCL), `dotnet test`, `dotnet build`.

## Global Constraints

These are project-wide requirements from the spec — every task implicitly obeys them.

- **Base branch**: v1.6.4 squash `8493ce4` (current main). `git checkout -b feature/v1-6-5-patch` from main.
- **`SendService` is `partial class` (not sealed)** — decorator inherits via `: SendService` override. **Do NOT modify `SendService`**.
- **5 UI callers' field types stay `SendService`** — DI resolves decorator via polymorphism. **Do NOT change `CanApi`, `SendViewModel`, `DbcSendViewModel`, `CyclicSendService`, `CyclicDbcSendService` field types or ctor signatures**.
- **Replay + IsoTp inject `CoreSendService` (raw, exempt)** — DI registration change, not ctor signature change (subclass assignment via inheritance).
- **xUnit 2.9.3** — `[Fact]` only; no `[Retry]` attribute (v1.6.1 PATCH Decision 5: 0-NuGet deps).
- **Token bucket clock source**: `Stopwatch.GetTimestamp()` (monotonic) — never `DateTime.UtcNow`.
- **Rejection returns `Result.Fail(ErrorCode.HardwareBusy, ...)`** — reuse existing enum value (no new error code).
- **Lock scope**: `lock(this)` protects only bucket state; `inner.SendAsync(frame, ct)` is dispatched outside the lock.
- **Test counts baseline**: Core 345 / App 407 / Infra 84 / Total 836 (6 SKIP). Target after v1.6.5: Core 345 / App 414 / Infra 84 / Total **843** (+7). 1 new file.
- **TDD discipline**: every test must fail (RED) before the implementation that makes it pass (GREEN).
- **Commit cadence**: 3 task commits (RED test → GREEN impl → review-fixup). Conventional commits (`feat:`, `fix:`, `test:`, `refactor:`, `docs:`, `chore:`).
- **Ship method**: 3 task commits → feature branch → `gh pr create` → `gh pr merge --squash --delete-branch` → `git fetch origin main` + `git reset --hard origin/main` → `git tag v1.6.5` → `gh release create`. Network: proxy ON per v1.6.3 + v1.6.4 ship notes.
- **Pre-ship**: `code-reviewer` subagent must report 0C/0H before merge. MEDIUM findings addressed inline; LOW accepted into known follow-ups.
- **Production-restriction-fixture-migration (8th sub-shape of phase-2-5-brief-drift-correction)**: Item 1 does not introduce path/network/process restriction. PR plan Task 0 must `git grep -n "Path.GetTempPath\|Path.Combine.*Temp\|Guid.NewGuid" tests/` and confirm 0 fixture migration expected.

---

## File Structure (locked in before tasks)

### Production files modified (3)

| File | Item | Change |
|---|---|---|
| `src/PeakCan.Host.App/Services/RateLimitedSendService.cs` | Item 1 | NEW: `internal sealed class : SendService`, override `SendAsync` with token-bucket check + delegate to inner. ~80 lines. |
| `src/PeakCan.Host.App/Composition/CoreSendService.cs` | Item 1 | NEW: `internal sealed class : SendService`, raw instance for Replay + IsoTp exempt. ~10 lines. |
| `src/PeakCan.Host.App/Composition/AppHostBuilder.cs` | Item 1 | MODIFY line 167 + adjacent: register `CoreSendService` (raw) + register `SendService` as decorator factory; update `ReplayFrameSinkAdapter` + `IsoTpLayer` registration to inject `CoreSendService`. |
| `appsettings.json` | Item 1 | MODIFY: add `"Send": { "MaxFramesPerSecond": 0 }` (default 0 = unlimited). |

### Test files created (1)

| File | Item | Change |
|---|---|---|
| `tests/PeakCan.Host.App.Tests/Services/RateLimitedSendServiceTests.cs` | Item 1 | NEW: 7 tests (`FakeSendService` subclass + AAA structure + FluentAssertions). |

### Docs created (3)

| File | Item | Purpose |
|---|---|---|
| `docs/superpowers/specs/2026-06-30-v1-6-5-patch-design.md` | Pre-flight | Design spec (this run's pre-task output) |
| `docs/superpowers/plans/2026-06-30-v1-6-5-patch.md` | Pre-flight | Implementation plan (this file) |
| `docs/release-notes-v1.6.5.md` | Final | Release notes (mirror v1.6.4 format) |

### Files explicitly NOT touched

| File | Why |
|---|---|
| `src/PeakCan.Host.App/Services/SendService.cs` | Already `partial class` + `virtual SendAsync`; decorator inherits. No change. |
| `src/PeakCan.Host.App/Services/Scripting/CanApi.cs` | Field type stays `SendService`; DI resolves decorator transparently. |
| `src/PeakCan.Host.App/ViewModels/SendViewModel.cs` | Field type stays `SendService`; existing FAIL pattern (line 202-204) auto-displays `HardwareBusy` rate-limit reject. |
| `src/PeakCan.Host.App/ViewModels/DbcSendViewModel.cs` | Same as SendViewModel. |
| `src/PeakCan.Host.App/Services/CyclicSendService.cs` | Field type stays `SendService`; receives decorator transparently. |
| `src/PeakCan.Host.App/Services/CyclicDbcSendService.cs` | Same. |
| `src/PeakCan.Host.Core/ErrorCode.cs` | Reuse `HardwareBusy = 7`. No new enum value. |
| `src/PeakCan.Host.Core/Result.cs` | Reuse `Result.Fail`. No new helper. |

---

## Task 0: Pre-flight — test fixture migration check + baseline verification

**Files:** None modified.

**Context for implementer:** Item 1 doesn't introduce path/network/process restriction, so expected 0 fixture migration. PR plan Task 0 verifies this assumption + captures baseline. Per v1.6.4 PATCH lesson (8th sub-shape of phase-2-5-brief-drift-correction), production restrictions force test fixture migration.

- [ ] **Step 1: Verify main is at v1.6.4 squash**
