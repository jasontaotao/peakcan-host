# v1.2.10 PATCH Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v1.2.10 PATCH fixing Stats chart 不 redraw、Signal plot toggle 竞态、Stats Legend 缺失、StatsViewModelTests flake 四个 PATCH-class 项目。

**Architecture:** 全部 PATCH 量级，零 API 变更。每项 1-2 个 TDD commits（RED 测试 + GREEN 修复）。生产代码改 2 个文件 + 测试改 2 个文件 + 1 个 release notes。

**Tech Stack:** .NET 10 + WPF + OxyPlot.Wpf 2.2.0 + xunit + CommunityToolkit.Mvvm

## Global Constraints

- 测试覆盖率目标：≥ 80%（既有 baseline ~75%；本 PATCH 新增 4 测试 + 1 文档）
- Branch: `feature/v1-2-10-patch`（已创建）
- Base: main @ v1.2.9 `d4835da`
- 任何运行时改动必须有 RED 测试先 commit
- 不要改既有测试断言（除非断言错了）
- 注释：业务 / 用户面向用**中文**，API / 协议 / OxyPlot 字段用**英文**
- Ship 前必须跑 code-reviewer subagent + 修 HIGH / MEDIUM

## File Structure

**Modify:**
- `src/PeakCan.Host.App/ViewModels/StatsViewModel.cs` — Item A 加 `PlotModel.InvalidatePlot(updateData: true)` 到 Apply 末尾；Item C ctor 加 `PlotModel.Legends.Add(new Legend {...})`
- `src/PeakCan.Host.App/ViewModels/SignalViewModel.cs` — Item B 加 public `HandlePlotCheckboxClick(SignalEntry entry, bool isChecked)` 方法
- `src/PeakCan.Host.App/Views/SignalView.xaml.cs` — Item B 改 `OnPlotCheckboxClick` 读 `cb.IsChecked`
- `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs` — Item A / C / D 测试 + ctor 加 `LeakedApplicationReset.CleanupLeakedApplication()`

**Create:**
- `tests/PeakCan.Host.App.Tests/ViewModels/SignalViewModelClickHandlerTests.cs` — Item B 测试
- `docs/release-notes-v1.2.10.md` — ship notes

**Read-only references (do not modify):**
- `src/PeakCan.Host.App/ViewModels/SignalChartViewModel.cs` — Item A 的 InvalidatePlot 参照
- `tests/PeakCan.Host.App.Tests/ViewModels/SignalViewModelTests.cs` — Item B 测试模式参照

---

### Task A: Stats chart 不 redraw (RED)

**Files:**
- Modify: `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs`
- Test: `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs`

**Interfaces:**
- Uses: `StatsViewModel.Push(BusStatistics)`, `StatsViewModel.PlotModel`, `PeakCan.Host.Infrastructure.Statistics.BusStatistics`
- `BusStatistics` 是 record `(long TotalFrames, long ErrorFrames, double FramesPerSecond, double BusLoadPercent)`（参照 StatsViewModel.cs:204-206 用法；如签名不符，按现有测试用法调整）

- [ ] **Step 1: 找到 StatsViewModelTests.cs 中的 Apply_Trigger_ 相关测试位置**

读 `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs`，找到现有的"Apply 系列"测试位置（v1.2.9 加的）。如果现有测试是 `Apply_*` 命名，新测试叫 `Apply_Triggers_PlotModel_Updated_Event`。

- [ ] **Step 2: 写 RED 测试**

在 `StatsViewModelTests` 类内加：

```csharp
[Fact]
public void Apply_Triggers_PlotModel_Updated_Event()
{
    // Arrange
    var vm = new StatsViewModel();
    var updatedCount = 0;
    vm.PlotModel.Updated += (sender, args) => updatedCount++;

    // Act — Push 直接走 inline Apply path（test context 无 Application，RunOnUiPost fall-through）
    vm.Push(new BusStatistics(TotalFrames: 1, ErrorFrames: 0, FramesPerSecond: 100.0, BusLoadPercent: 5.0));

    // Assert — Apply 应触发 OxyPlot 的 Updated event（即 InvalidatePlot 被调用）
    Assert.True(updatedCount >= 1, $"Expected PlotModel.Updated to fire at least once after Push; got {updatedCount}.");
}
```

如果 `BusStatistics` 不是 record 而是 class 或有不同构造签名，按现有测试用法调。例如：

```csharp
new BusStatistics { TotalFrames = 1, ErrorFrames = 0, FramesPerSecond = 100.0, BusLoadPercent = 5.0 }
```

用 `git grep BusStatistics tests/` 找现有的实例化方式。

- [ ] **Step 3: 跑测试确认 RED**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~Apply_Triggers_PlotModel_Updated_Event" --no-build
```

Expected: FAIL — `updatedCount` 是 0（因为 Apply 没调 InvalidatePlot）。

- [ ] **Step 4: Commit RED**

```bash
cd D:/claude_proj2/peakcan-host && git add tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs && git commit -m "test(stats): RED — Apply_Triggers_PlotModel_Updated_Event

v1.2.9 PATCH fixed Stats chart Points 重填，但漏掉 PlotModel.InvalidatePlot 调用，
导致 OxyPlot 2.2.0 不重绘。订阅 PlotModel.Updated event 验证 InvalidatePlot 是否被触发。"
```

---

### Task A.2: Stats chart 不 redraw (GREEN)

**Files:**
- Modify: `src/PeakCan.Host.App/ViewModels/StatsViewModel.cs:204-233` (Apply method 末尾)

- [ ] **Step 1: 加 1 行 InvalidatePlot**

读 `src/PeakCan.Host.App/ViewModels/StatsViewModel.cs`，定位到 `Apply` 方法末尾（line 233 之前）。在最后的 `}` 之前加：

```csharp
        // v1.2.10: OxyPlot 2.2.0 WPF doesn't auto-invalidate on LineSeries.Points
        // mutation; without explicit InvalidatePlot the chart stays frozen at the
        // constructor pre-fill. updateData:true forces OxyPlot to re-process Points.
        PlotModel.InvalidatePlot(updateData: true);
```

- [ ] **Step 2: 跑 RED 测试确认 GREEN**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~Apply_Triggers_PlotModel_Updated_Event" --no-restore
```

Expected: PASS.

- [ ] **Step 3: 跑全 StatsViewModelTests 确认无 regression**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~StatsViewModelTests" --no-restore
```

Expected: ALL PASS（baseline 已有 ~7 个 StatsViewModelTests，新增 1 个）。

- [ ] **Step 4: 跑全 App.Tests**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --no-restore
```

Expected: ALL PASS + 无新增 SKIP + 无新增 fail。

- [ ] **Step 5: Commit GREEN**

```bash
cd D:/claude_proj2/peakcan-host && git add src/PeakCan.Host.App/ViewModels/StatsViewModel.cs && git commit -m "fix(stats): call PlotModel.InvalidatePlot in Apply

v1.2.9 PATCH closed the X-index bug but left the chart frozen because
OxyPlot 2.2.0 WPF doesn't auto-invalidate on LineSeries.Points.Clear+Add.
Mirror SignalChartViewModel.OnRenderTick which already calls
InvalidatePlot(true) after Points mutation.

Updated event subscription in StatsViewModelTests catches the regression;
manual smoke (Stats tab + connected CAN) confirms 1 Hz scroll."
```

---

### Task B: Signal Click handler 竞态 (RED)

**Files:**
- Create: `tests/PeakCan.Host.App.Tests/ViewModels/SignalViewModelClickHandlerTests.cs`
- Test: same

**Interfaces:**
- 新 `SignalViewModel.HandlePlotCheckboxClick(SignalEntry entry, bool isChecked)` — 简单 delegate 到 `OnSignalSelectionChanged`
- 测试用真实 `SignalChartViewModel`，行为断言通过 `chartVm.SignalCount` 和 `chartVm.PlotModel.Series.Count`

> **Pre-flight note**: 原 plan 用 `SpyChartViewModel` + `new` shadowing 是错的 —— `SignalViewModel._chartVm` 静态类型是 `SignalChartViewModel`，non-virtual + `new` shadowing 时 base 引用仍调 base 方法，spy 永远不会被触达。改用真实 chart VM + `SignalCount` 行为断言，更接近用户视角 bug。

- [ ] **Step 1: 写 RED 测试**

创建 `tests/PeakCan.Host.App.Tests/ViewModels/SignalViewModelClickHandlerTests.cs`：

```csharp
using PeakCan.Host.App.ViewModels;
using Xunit;

namespace PeakCan.Host.App.Tests.ViewModels;

/// <summary>
/// v1.2.10: regression tests for the Signal plot toggle Click handler.
/// Pre-fix the handler read sender.DataContext.IsSelected from the
/// row's SignalEntry — but DrainPending replaces Latest[i] every frame,
/// and the row's DataContext can target the NEW entry (whose IsSelected
/// was preserved=true from the OLD entry the user just unchecked). The
/// fix routes through HandlePlotCheckboxClick(entry, cb.IsChecked) so
/// the UI-side toggle value drives the AddSignal/RemoveSignal call.
/// </summary>
public sealed class SignalViewModelClickHandlerTests
{
    [Fact]
    public void HandlePlotCheckboxClick_True_Adds_Signal_To_Chart()
    {
        // Arrange: real chart VM + entry added to Latest
        var chartVm = new SignalChartViewModel();
        var vm = new SignalViewModel(chartVm);
        var entry = new SignalEntry { Message = "M2", Signal = "S2" };
        vm.Latest.Add(entry);

        // Act: handler invoked with cb.IsChecked = true (user just checked)
        vm.HandlePlotCheckboxClick(entry, isChecked: true);

        // Assert: chart VM now has 1 signal
        Assert.Equal(1, chartVm.SignalCount);
        Assert.Equal(3, chartVm.PlotModel.Series.Count);  // 1 new + 2 reserved (none here, just our add)
        // Actually SignalChartViewModel ctor doesn't pre-add series, so 1 total
    }

    [Fact]
    public void HandlePlotCheckboxClick_False_After_Entry_Replacement_Removes_From_Chart()
    {
        // Arrange: real chart VM, signal added
        var chartVm = new SignalChartViewModel();
        var vm = new SignalViewModel(chartVm);
        var entry = new SignalEntry { Message = "M1", Signal = "S1" };
        vm.Latest.Add(entry);

        // First: user checked → AddSignal called → chart has 1
        vm.HandlePlotCheckboxClick(entry, isChecked: true);
        Assert.Equal(1, chartVm.SignalCount);

        // Simulate DrainPending's Upsert replacing Latest[i]:
        //   entry.IsSelected = Latest[i].IsSelected (=true)  ← preserve
        //   Latest[i] = newEntry                              ← replace
        // The NEW entry has IsSelected=true (preserved from OLD).
        var newEntry = new SignalEntry
        {
            Message = "M1",
            Signal = "S1",
            IsSelected = true,
        };
        vm.Latest[0] = newEntry;

        // Act: user clicks to UNCHECK (cb.IsChecked = false).
        // Pre-fix: handler read entry.IsSelected = true → AddSignal called (wrong)
        // Post-fix: handler reads cb.IsChecked = false → RemoveSignal called (correct)
        vm.HandlePlotCheckboxClick(newEntry, isChecked: false);

        // Assert: chart VM signal count is 0 (line removed)
        Assert.Equal(0, chartVm.SignalCount);
    }

    [Fact]
    public void HandlePlotCheckboxClick_False_On_Never_Added_Signal_Is_Noop()
    {
        // Arrange: chart VM exists but signal never added
        var chartVm = new SignalChartViewModel();
        var vm = new SignalViewModel(chartVm);
        var entry = new SignalEntry { Message = "M3", Signal = "S3" };
        vm.Latest.Add(entry);

        // Act: user unchecks without ever checking first
        vm.HandlePlotCheckboxClick(entry, isChecked: false);

        // Assert: no exception, chart still empty
        Assert.Equal(0, chartVm.SignalCount);
    }
}
```

**注意**：第二个测试 (`HandlePlotCheckboxClick_False_After_Entry_Replacement_Removes_From_Chart`) 就是 RED 的核心 —— 在 pre-fix 的 handler 实现下（读 `entry.IsSelected`），这一行 `vm.HandlePlotCheckboxClick(newEntry, isChecked: false)` 会因为 `newEntry.IsSelected=true` 而**误调 AddSignal**，断言 `SignalCount == 0` 会 fail（实际是 2）。这个 test 是 fix 前后行为差异的回归网。

- [ ] **Step 2: 跑测试确认 RED（应编译失败）**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~SignalViewModelClickHandlerTests" --no-restore
```

Expected: COMPILE FAIL — `SignalViewModel.HandlePlotCheckboxClick` 不存在。

- [ ] **Step 3: Commit RED**

```bash
cd D:/claude_proj2/peakcan-host && git add tests/PeakCan.Host.App.Tests/ViewModels/SignalViewModelClickHandlerTests.cs && git commit -m "test(signal): RED — Click handler ignores stale entry.IsSelected

v1.2.10 PATCH: regression test for the race where DrainPending replaces
SignalEntry in Latest[i] between the user's click and the Click handler
reading sender.DataContext. Pre-fix the handler reads entry.IsSelected
from the NEW entry (preserved=true), so an uncheck click was treated
as a check and AddSignal was called instead of RemoveSignal.

SpyChartViewModel counts AddSignal/RemoveSignal calls so the test is
deterministic regardless of OxyPlot's internal state."
```

---

### Task B.2: Signal Click handler 竞态 (GREEN)

**Files:**
- Modify: `src/PeakCan.Host.App/ViewModels/SignalViewModel.cs` — 加 `HandlePlotCheckboxClick` 方法
- Modify: `src/PeakCan.Host.App/Views/SignalView.xaml.cs:53-60` — Click handler 改读 `cb.IsChecked`

- [ ] **Step 1: 加 SignalViewModel.HandlePlotCheckboxClick 方法**

读 `src/PeakCan.Host.App/ViewModels/SignalViewModel.cs`，在 `OnSignalSelectionChanged` 之后（line 441 之后）加：

```csharp
    /// <summary>
    /// v1.2.10: routing layer for the Signal tab's per-row Plot checkbox.
    /// Takes the checkbox's UI-side IsChecked (just toggled by the click)
    /// rather than the SignalEntry's source-side IsSelected (which can be
    /// stale because DrainPending replaces the entry in Latest[i] every
    /// frame, and the row's DataContext can target the NEW entry by the
    /// time the Click handler runs). Centralised here so unit tests can
    /// verify the routing without spinning up a WPF UserControl.
    /// </summary>
    public void HandlePlotCheckboxClick(SignalEntry entry, bool isChecked)
        => OnSignalSelectionChanged(entry.Message, entry.Signal, isChecked);
```

**实现细节**：pre-fix 代码直接读 `entry.IsSelected`（line 58 in old SignalView.xaml.cs），所以即使 `HandlePlotCheckboxClick` 接 `isChecked` 参数，如果实现错了写成 `OnSignalSelectionChanged(entry.Message, entry.Signal, entry.IsSelected)`，RED 测试就会 catch。

- [ ] **Step 3: 跑 RED 测试确认 GREEN**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~SignalViewModelClickHandlerTests" --no-restore
```

Expected: PASS (2 tests).

- [ ] **Step 4: 跑全 SignalViewModelTests 确认无 regression**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~SignalViewModelTests" --no-restore
```

Expected: ALL PASS（既有 `OnSignalSelectionChanged_*` 测试不依赖新方法）。

- [ ] **Step 5: 跑全 App.Tests**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --no-restore
```

Expected: ALL PASS.

- [ ] **Step 6: Commit GREEN**

```bash
cd D:/claude_proj2/peakcan-host && git add src/PeakCan.Host.App/ViewModels/SignalViewModel.cs src/PeakCan.Host.App/Views/SignalView.xaml.cs && git commit -m "fix(signal): read CheckBox.IsChecked in Click handler

v1.2.10: DrainPending replaces Latest[i] with a fresh SignalEntry every
frame (Upsert preserves IsSelected across the swap, but the row's
DataContext retargets to the NEW entry). The Click handler then reads
sender.DataContext.IsSelected from the NEW entry — true, preserved from
OLD — and mistakenly calls AddSignal when the user clicked to uncheck.

Read sender.IsChecked (UI-side DP, definitively the user's just-toggled
intent) and route through a new HandlePlotCheckboxClick so the
decision path is unit-testable without WPF.

Manual smoke (DBC loaded + flowing CAN + click checkboxes): chart
series and visual now stay in sync."
```

---

### Task C: Stats Legend (RED)

**Files:**
- Modify: `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs`

- [ ] **Step 1: 写 RED 测试**

读 `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs`，找到构造相关测试或尾部，加：

```csharp
[Fact]
public void Constructor_Adds_Legend_To_PlotModel()
{
    // Arrange + Act
    var vm = new StatsViewModel();

    // Assert
    Assert.NotEmpty(vm.PlotModel.Legends);
    var legend = vm.PlotModel.Legends[0];
    Assert.Equal(OxyPlot.Legends.LegendPlacement.Outside, legend.LegendPlacement);
}
```

如果 `OxyPlot.Legends` namespace 不在 default usings，加 `using OxyPlot.Legends;`。

- [ ] **Step 2: 跑测试确认 RED**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~Constructor_Adds_Legend_To_PlotModel" --no-build
```

Expected: FAIL — `vm.PlotModel.Legends` 是空集合。

- [ ] **Step 3: Commit RED**

```bash
cd D:/claude_proj2/peakcan-host && git add tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs && git commit -m "test(stats): RED — Constructor_Adds_Legend_To_PlotModel

v1.2.10: v1.2.9 release notes flagged Stats tab Legend empty as a
carry-over; OxyPlot 2.2.0 doesn't render legend by default."
```

---

### Task C.2: Stats Legend (GREEN)

**Files:**
- Modify: `src/PeakCan.Host.App/ViewModels/StatsViewModel.cs` (constructor)

- [ ] **Step 1: 加 Legend 到 ctor**

读 `src/PeakCan.Host.App/ViewModels/StatsViewModel.cs` 构造函数，定位到 `PlotModel.Series.Add(_loadLine);` 之后（line 180 之后）。加：

```csharp
        // v1.2.10: OxyPlot 2.2.0 doesn't render series labels unless a
        // Legend is registered on the PlotModel. Without this the FPS /
        // Bus-load series names show up only as data, no legend.
        PlotModel.Legends.Add(new OxyPlot.Legends.Legend
        {
            LegendPlacement = OxyPlot.Legends.LegendPlacement.Outside,
            LegendPosition = OxyPlot.Legends.LegendPosition.RightTop,
            LegendBackground = OxyColor.FromAColor(32, OxyColor.FromRgb(0xFF, 0xFF, 0xFF)),
            LegendBorder = OxyColor.FromRgb(0xCC, 0xCC, 0xCC),
            LegendBorderThickness = 1,
        });
```

如果 `OxyPlot.Legends` namespace 不在 default usings，在文件顶部加 `using OxyPlot.Legends;`。

- [ ] **Step 2: 跑 RED 测试确认 GREEN**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~Constructor_Adds_Legend_To_PlotModel" --no-build
```

Expected: PASS.

- [ ] **Step 3: 跑全 StatsViewModelTests**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~StatsViewModelTests" --no-build
```

Expected: ALL PASS.

- [ ] **Step 4: Commit GREEN**

```bash
cd D:/claude_proj2/peakcan-host && git add src/PeakCan.Host.App/ViewModels/StatsViewModel.cs && git commit -m "feat(stats): render FPS + Bus-load series labels in Legend

v1.2.10: v1.2.9 carry-over. OxyPlot 2.2.0 requires explicit Legend
registration on PlotModel or series labels don't render. Outside
placement + RightTop keeps the chart area unchanged."
```

---

### Task D: StatsViewModelTests flake (fix only)

**Files:**
- Modify: `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs` (ctor)

- [ ] **Step 1: 加 LeakedApplicationReset 到 ctor**

读 `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs` 顶部，找到类的 ctor。如果 ctor 不存在，加：

```csharp
    public StatsViewModelTests()
    {
        // v1.2.10: parallel-execution Application leak guard. v1.2.1 PATCH
        // added the same cleanup to 4 sibling test classes (SignalVM,
        // StatsVM, TraceVM, DbcDecodeBG) to fix the same flake. v1.2.9
        // closed the leak in StatsVM partial but missed this ctor — the
        // Push_Trims_Series_To_MaxPoints_After_Many_Samples test still
        // flakes once every ~50 runs in full-suite.
        LeakedApplicationReset.CleanupLeakedApplication();
    }
```

如果 ctor 已存在（如继承 IDisposable 等），把 `LeakedApplicationReset.CleanupLeakedApplication();` 加到 ctor 第一行。

- [ ] **Step 2: 跑全 StatsViewModelTests 5 次验证 flake 消失**

```bash
cd D:/claude_proj2/peakcan-host && for i in 1 2 3 4 5; do dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --filter "FullyQualifiedName~StatsViewModelTests" --no-build 2>&1 | tail -5; done
```

Expected: 5/5 全部 PASS。

- [ ] **Step 3: 跑全 App.Tests 1 次**

```bash
cd D:/claude_proj2/peakcan-host && dotnet test tests/PeakCan.Host.App.Tests/PeakCan.Host.App.Tests.csproj --no-build
```

Expected: ALL PASS。

- [ ] **Step 4: Commit**

```bash
cd D:/claude_proj2/peakcan-host && git add tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs && git commit -m "test(stats): add LeakedApplicationReset.CleanupLeakedApplication to ctor

v1.2.10: v1.2.9 release notes flagged Push_Trims_Series_To_MaxPoints_After_Many_Samples
as a parallel-execution flake (~1 in 50 runs in full-suite). Root cause
identical to the v1.2.1 PATCH pattern: xunit parallel execution leaks
the STA Application.Current between test classes. v1.2.1 already applied
the CleanupLeakedApplication guard to SignalVM/StatsVM/TraceVM/DbcDecodeBG
test classes; this commit closes the same gap on StatsViewModelTests."
```

---

### Task E: Release notes + pre-ship review

**Files:**
- Create: `docs/release-notes-v1.2.10.md`
- Read: 整个 `feature/v1-2-10-patch` diff

- [ ] **Step 1: 写 release notes**

创建 `docs/release-notes-v1.2.10.md`：

```markdown
# Release Notes — PeakCan Host v1.2.10

**Date:** 2026-06-26

## Summary

v1.2.10 is a 4-item PATCH that fixes two user-reported bugs from v1.2.9
plus two carry-overs from the v1.2.9 release notes.

## Bug fixes

### Stats chart 不 redraw（v1.2.9 回归）

`StatsViewModel.Apply` 重建 `_fpsLine.Points` 但没调 `PlotModel.InvalidatePlot`，
OxyPlot 2.2.0 WPF 不会自动重绘。修复：Apply 末尾加 `PlotModel.InvalidatePlot(updateData: true)`。

对照 `SignalChartViewModel.OnRenderTick`（已有正确实现）。

### Signal plot toggle 竞态

`OnPlotCheckboxClick` 读 `entry.IsSelected`，但 `DrainPending` 每帧替换 Latest[i]
（Upsert 创建新 SignalEntry 实例），sender.DataContext 可能已 target 到 NEW entry
（其 IsSelected 被 preserve 为 true），导致 uncheck 点击误触发 AddSignal。

修复：读 `((CheckBox)sender).IsChecked`（UI 侧 DP，用户刚 toggle 的新值），
经新增的 `HandlePlotCheckboxClick(entry, isChecked)` 路由到 `OnSignalSelectionChanged`。

无数据时无 DrainPending 替换，原 bug 不触发；用户报告一致。

## Carry-overs from v1.2.9

### Stats OxyPlot Legend

v1.2.9 release notes §"Known issue" 已记。修复：`PlotModel.Legends.Add(new Legend {...})`，
`LegendPlacement = Outside`，`LegendPosition = RightTop`。

### StatsViewModelTests parallel flake

v1.2.9 release notes §"Test flake note" 已记。修复：ctor 加 `LeakedApplicationReset.CleanupLeakedApplication()`，
参照 v1.2.1 PATCH 模式。

## Tests

+ 3 tests:
- `StatsViewModelTests.Apply_Triggers_PlotModel_Updated_Event`（Item A）
- `StatsViewModelTests.Constructor_Adds_Legend_To_PlotModel`（Item C）
- `SignalViewModelClickHandlerTests.HandlePlotCheckboxClick_False_After_Entry_Replacement_Calls_RemoveSignal`
- `SignalViewModelClickHandlerTests.HandlePlotCheckboxClick_True_Calls_AddSignal`

Item D 无新测试 — 是 test infra cleanup（v1.2.1 同样的修复也无新测试）。

## Files changed

- `src/PeakCan.Host.App/ViewModels/StatsViewModel.cs` — Item A + C
- `src/PeakCan.Host.App/ViewModels/SignalViewModel.cs` — Item B（HandlePlotCheckboxClick）
- `src/PeakCan.Host.App/Views/SignalView.xaml.cs` — Item B（OnPlotCheckboxClick）
- `tests/PeakCan.Host.App.Tests/ViewModels/StatsViewModelTests.cs` — Item A/C tests + Item D ctor
- `tests/PeakCan.Host.App.Tests/ViewModels/SignalViewModelClickHandlerTests.cs` — new, Item B tests

## Public API

无变化。`HandlePlotCheckboxClick` 是 public 方法（必要 for test + reuse from view），
不破坏现有 `OnSignalSelectionChanged` 公共契约。

## Known issues

无新增。v1.3.0 MINOR backlog（OEM `IKeyDerivationAlgorithm` concrete impls）继续阻塞在
OEM 清单到位。

## Ship mechanics

`git push origin feature/v1-2-10-patch` + PR 合并 + `git tag v1.2.10` + `gh release create v1.2.10`。
```

- [ ] **Step 2: 整批 code-reviewer**

```bash
cd D:/claude_proj2/peakcan-host && git log --oneline main..HEAD
```

确认 commits：
```
fix(test): add LeakedApplicationReset to StatsViewModelTests ctor
feat(stats): render FPS + Bus-load series labels in Legend
test(stats): RED - Constructor_Adds_Legend_To_PlotModel
fix(signal): read CheckBox.IsChecked in Click handler
test(signal): RED - Click handler ignores stale entry.IsSelected
fix(stats): call PlotModel.InvalidatePlot in Apply
test(stats): RED - Apply_Triggers_PlotModel_Updated_Event
```

然后 dispatch `code-reviewer` subagent（用 `general-purpose` agent，按 ECC rules/ecc 流程）：

```
Review the feature/v1-2-10-patch branch (8 commits, +~80 lines) for v1.2.10 PATCH.
4 items: Stats chart redraw, Signal Click handler race, Stats Legend, StatsVM flake.
Focus on:
- Item A: PlotModel.InvalidatePlot placement + updateData:true rationale
- Item B: cb.IsChecked vs entry.IsSelected correctness; HandlePlotCheckboxClick test coverage
- Item C: Legend placement not breaking chart layout
- Item D: ctor placement of LeakedApplicationReset.CleanupLeakedApplication
Severity scale CRITICAL / HIGH / MEDIUM / LOW. Report per item.
```

修 HIGH / MEDIUM（每个 fix 一个 commit）。

- [ ] **Step 3: 全量 build + test**

```bash
cd D:/claude_proj2/peakcan-host && dotnet build && dotnet test --no-build
```

Expected: 0 warn 0 err, ALL PASS。

- [ ] **Step 4: Push + PR + squash + tag + release**

```bash
cd D:/claude_proj2/peakcan-host && git -c http.proxy="" push origin feature/v1-2-10-patch
```

如果 `github.com:443` 阻塞（v1.2.9 备忘），走 `gh api` 7-payload workflow（参照 memory）。

PR 合并：
```bash
gh pr create --title "v1.2.10 PATCH: Stats redraw + Signal Click race + Legend + flake fix" --body-file docs/release-notes-v1-2-10-pr-body.md
gh pr merge --squash --delete-branch
```

Tag + release：
```bash
git tag -a v1.2.10 -m "v1.2.10: Stats chart redraw + Signal Click race + Legend + test flake"
git push origin v1.2.10
gh release create v1.2.10 --title "PeakCan Host v1.2.10" --notes-file docs/release-notes-v1.2.10.md
```

- [ ] **Step 5: 更新 memory + 更新 MEMORY.md**

写 `peakcan-host-v1-2-10-shipped.md` memory + 在 `MEMORY.md` 加一行指针。

---

## Self-Review Checklist（plan 完成前自检）

- [x] Spec coverage：4 个 item 各有任务（A.1+A.2, B.1+B.2, C.1+C.2, D）
- [x] Placeholder scan：所有代码块都是实际代码，不是 "TBD" / "implement later"
- [x] Type consistency：`BusStatistics`, `SignalEntry`, `CheckBox`, `Legend` 用法与既有代码一致
- [x] TDD：每个 fix 都有 RED 测试先行
- [x] Frequent commits：每个 step 都是独立 commit
- [x] Public API 变化只新增 1 个 public 方法（`HandlePlotCheckboxClick`），已在 spec 注明
- [x] Ship 流程：review → fix HIGH/MEDIUM → push → PR → tag → release