# v1.7.4 PATCH Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship 1-item Option B housekeeping: commit v1.7.3 cycle's 2 untracked design/plan docs (held back per Option B convention).

**Architecture:** Doc-only commit. No production code change. No test change. 9th consecutive Option B housekeeping PATCH.

**Tech Stack:** N/A (no code changes).

---

## Global Constraints

These constraints apply to **every task** below. Bind from the v1.7.4 PATCH spec
(`docs/superpowers/specs/2026-07-01-v1-7-4-patch-design.md`) and the v1.6.10 +
v1.7.0 + v1.7.1 + v1.7.2 + v1.7.3 PATCH precedents:

1. **Branch base**: `git checkout -b feature/v1-7-4-patch origin/main`
   (`origin/main` = `f62070b`, v1.7.3 squash via Tier 3). NOT local `main`
   (intentionally held at `5c522ca` v1.7.0 revert per v1.7.1 + v1.7.2 + v1.7.3
   PATCH ship precedent).
2. **Local `main` revert preservation**: do NOT run `git reset --hard origin/main`
   during this cycle. Local `main` is intentionally at `5c522ca`.
3. **Option B convention**: this cycle's own design + plan docs are HELD BACK
   (not committed during v1.7.4). They become v1.7.5 PATCH Item.
4. **TDD-first**: N/A (no production code change).
5. **Commit format**: conventional commits (`docs:` for housekeeping).
6. **Test coverage floor**: N/A (no test change). Baseline 875 + 6 SKIP.
7. **Code-review target**: 0C/0H/0M/0L (doc-only change).
8. **Ship network**: github.com:443 may still be flaky from v1.7.3 PATCH Tier 3
   ship. Re-verify before push per git-push-network-workaround.md. If sustained
   down, use Tier 3 fallback (full gh api path, 11 calls per cycle).
9. **Race-test flake** (14-of-14+ confirmed v1.6.2 → v1.7.3 PATCH): pre-existing
   transient flaky race tests. Pass in isolation = pre-existing flake, not
   regression. `[Retry(3)]` reversal: explicitly rejected v1.6.1 PATCH
   Decision 5.

---

## File structure

Files modified in this PATCH:

| File | Role | Change scope |
|---|---|---|
| `docs/superpowers/specs/2026-07-01-v1-7-3-patch-design.md` | v1.7.3 PATCH spec (Option B carry-over) | Commit only (currently untracked) |
| `docs/superpowers/plans/2026-07-01-v1-7-3-patch.md` | v1.7.3 PATCH plan (Option B carry-over) | Commit only (currently untracked) |
| `docs/release-notes-v1.7.4.md` | v1.7.4 PATCH release notes | New file |

---

### Task 1: Option B housekeeping (commit v1.7.3 cycle's 2 untracked docs)

**Files:**
- Commit only (no content change):
  - `docs/superpowers/specs/2026-07-01-v1-7-3-patch-design.md`
  - `docs/superpowers/plans/2026-07-01-v1-7-3-patch.md`

**Interfaces:**
- Consumes: nothing
- Produces: 2 files moved from untracked → tracked on `feature/v1-7-4-patch`

- [ ] **Step 1: Verify the 2 docs are untracked**

```bash
git status --porcelain | grep "^??" | grep "2026-07-01-v1-7-3-patch"
```

Expected output: 2 lines, one for each file:
```
?? docs/superpowers/plans/2026-07-01-v1-7-3-patch.md
?? docs/superpowers/specs/2026-07-01-v1-7-3-patch-design.md
```

- [ ] **Step 2: Stage the 2 docs**

```bash
git add docs/superpowers/specs/2026-07-01-v1-7-3-patch-design.md \
        docs/superpowers/plans/2026-07-01-v1-7-3-patch.md
```

- [ ] **Step 3: Verify staged diff**

```bash
git diff --cached --stat
```

Expected output: 2 files changed, ~27 KB total (7 KB spec + 20 KB plan).

- [ ] **Step 4: Commit**

```bash
git commit -m "docs(v1.7.4): commit v1.7.3 PATCH design + plan docs (Option B carry-over)"
```

---

### Task 2: Pre-ship verification + release notes

**Files:**
- Create: `docs/release-notes-v1.7.4.md` — user-facing release notes

**Interfaces:**
- Consumes: 1 commit from Task 1
- Produces: release notes doc ready to ship

- [ ] **Step 1: Skip full test suite (no production code change)**

This PATCH is doc-only. No test suite run needed. Baseline 875 + 6 SKIP unchanged.

- [ ] **Step 2: Write release notes**

Create `docs/release-notes-v1.7.4.md` with the following content:

```markdown
# v1.7.4 PATCH — Release Notes (2026-07-01)

## Summary

1-item Option B housekeeping: commit v1.7.3 PATCH cycle's untracked
design/plan docs (held back per Option B convention).

## What's changed

### Item 1 — Housekeeping

Committed 2 design/plan docs from the v1.7.3 PATCH cycle (held back per
the Option B convention that has now been followed for 9 consecutive PATCHes
since v1.6.6). v1.7.4 cycle's own design + plan docs are similarly held
back for v1.7.5 PATCH Item.

## Test counts

| Suite | v1.7.3 | v1.7.4 | Delta |
|-------|--------|--------|-------|
| Total | 875 + 6 SKIP | 875 + 6 SKIP | +0 net |

No production code change, no test change.

## Compatibility

No API changes.

## Migration

None required.

## Known follow-ups

- **v1.7.5 PATCH** (next): Option B housekeeping for v1.7.4 cycle's docs.
  No MEDIUMs deferred from v1.7.4 (doc-only change).
- **v1.7.1 MINOR** (future): OEM `IKeyDerivationAlgorithm` concrete
  implementation — the last v1.6.0 MINOR item. Needs crypto review first.
- **ClearScript 7.5+ bump**: would unlock V8-prefixed exception types.
- **Cross-cutting memory updates** (git-push-network-workaround Tier 3 doc,
  MEMORY.md rotation): handled separately per Option C follow-up cycle if
  user requests.

## Ship metadata

- PR: `feature/v1-7-4-patch` → `main` (squash) OR Tier 3 force-update
  if github.com:443 still flaky from v1.7.3 PATCH Tier 3 ship
- Tag: `v1.7.4`
- Branch base: `origin/main` @ `f62070b` (v1.7.3 squash)
- Local `main` revert to `5c522ca` (v1.7.0) preserved per v1.7.1 + v1.7.2 + v1.7.3 PATCH ship precedent.
```

- [ ] **Step 3: Commit release notes**

```bash
git add docs/release-notes-v1.7.4.md
git commit -m "docs(v1.7.4): release notes for v1.7.4 PATCH"
```

- [ ] **Step 4: Pre-ship code-reviewer dispatch**

Dispatch `code-reviewer` agent (haiku Tidy scope, doc-only) on the 2 commits:

```
Subagent: code-reviewer
Model: haiku (doc-only Tidy scope — small change)
Scope: feature/v1-7-4-patch (2 commits on top of origin/main f62070b)
Expected verdict: 0C/0H/0M/0L
Files in diff:
  - docs/superpowers/specs/2026-07-01-v1-7-3-patch-design.md (commit only)
  - docs/superpowers/plans/2026-07-01-v1-7-3-patch.md (commit only)
  - docs/release-notes-v1.7.4.md (new file)
```

If CRITICAL or HIGH findings: dispatch fix subagent. If MEDIUM/LOW: doc-only
in nature, defer to v1.7.5 if needed.

---

### Task 3: Ship v1.7.4 PATCH

**Files:**
- Push: `feature/v1-7-4-patch` → `origin` (or Tier 3 force-update)
- Tag: `v1.7.4` via `gh api repos/.../git/refs`
- Release: `gh release create v1.7.4 --target <squash-sha> --notes-file docs/release-notes-v1.7.4.md`

- [ ] **Step 1: Verify network state (re-verify, don't assume)**

```bash
curl -s -o /dev/null -w "github.com: %{http_code}\n" --max-time 5 https://github.com
curl -s -o /dev/null -w "api.github.com: %{http_code}\n" --max-time 5 https://api.github.com
```

If github.com:443 reachable → Tier 1/2 path.
If github.com:443 sustained down → Tier 3 fallback (full gh api path, 11 calls).

- [ ] **Step 2: Push branch (proxy bypass) OR Tier 3 force-update**

**Tier 1** (if github.com:443 reachable):
```bash
git -c http.proxy= -c https.proxy= push -u origin feature/v1-7-4-patch
```

**Tier 3** (if github.com:443 sustained down): per v1.7.3 PATCH Tier 3 precedent —
export 3 files (release-notes + 2 v1.7.3 docs) to Windows-native tmp, then
gh api blobs/tree/commit/ref-update (force=true)/tag/release.

- [ ] **Step 3: Create PR (Tier 1/2 only — Tier 3 bypasses PR)**

```bash
gh pr create \
    --title "v1.7.4 PATCH: Option B housekeeping (v1.7.3 docs)" \
    --body-file docs/release-notes-v1.7.4.md
```

- [ ] **Step 4: Merge PR (Tier 1/2 only)**

```bash
gh pr merge --squash --delete-branch
```

If this reports fatal but exit code is non-zero: check `gh pr view` for
actual merge state (Tier 2 path per v1.7.1 + v1.7.2 precedent).

- [ ] **Step 5: Create tag v1.7.4**

```bash
gh api repos/jasontaotao/peakcan-host/git/refs \
    -f ref="refs/tags/v1.7.4" \
    -f sha="<squash-sha>"
```

If Tier 3: tag is created in Tier 3 force-update sequence (Step 2).

- [ ] **Step 6: Create release**

```bash
gh release create v1.7.4 \
    --target <squash-sha> \
    --title "v1.7.4 PATCH" \
    --notes-file docs/release-notes-v1.7.4.md
```

- [ ] **Step 7: Verify ship on remote**

```bash
gh api repos/jasontaotao/peakcan-host/git/refs/tags/v1.7.4 --jq '.object.sha'
```

Expected: tag SHA matches the squash commit. Remote main should be 1
squash commit ahead of `f62070b` (v1.7.3).

- [ ] **Step 8: DO NOT reset local main**

Per Constraint #2: local `main` is intentionally at `5c522ca` (v1.7.0
revert). Do NOT run `git reset --hard origin/main`. The next cycle (v1.7.5
PATCH) will fork from `origin/main` again per the established pattern.

---

## Self-review

1. **Spec coverage**: Item 1 (Option B housekeeping): ✓ Task 1
   No MEDIUMs deferred: ✓ Task 2 Step 4
   Ship via Tier 1/2/3 fallback: ✓ Task 3
   **No gaps.**

2. **Placeholder scan**: no TBD / TODO / "similar to Task N" / etc. ✓

3. **Type consistency**: N/A (no code change). Commit format consistent:
   `docs(v1.7.4): ...` for housekeeping commits. ✓