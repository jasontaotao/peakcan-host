# PeakCan Host — SDD Progress Ledger

This ledger is the recovery map for subagent-driven development. Tasks listed here as `complete` are DONE — do not re-dispatch.

## Plan reference
- Spec: `D:/claude_proj2/docs/superpowers/specs/2026-06-18-peakcan-host-design.md`
- Plan: `D:/claude_proj2/docs/superpowers/plans/2026-06-18-peakcan-host-implementation.md`
- Repo: `D:/claude_proj2/peakcan-host/` (main branch, single-developer)
- Ledger updated: 2026-06-18

## Status

| Task | Status | Commits | Reviewer verdict |
|---|---|---|---|
| 1  scaffolding        | complete | feat: chore commit `scaffold 3-layer solution` | self-validated by `dotnet build` 0 warn 0 err |
| 1a plan deviations    | complete | inline in scaffolding | TFM net8→net10, LiveCharts2→OxyPlot.Wpf, Peak.Can.Basic→Peak.PCANBasic.NET |
| 2  Core domain types  | complete | `9ee928d` feat + `bd157e1` test(coverage) | 24/24 tests pass; 100% line / 100% branch coverage on Core; build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (1 MEDIUM coverage gap fixed) |
| 3  Result/Error/ErrorCode | complete | `8ffdb29` feat + polish commit | 32/32 tests pass; 100% line / 100% branch coverage on Core; build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (4 LOW docs/test polish applied) |
| 4  DbcTokenizer         | complete | `f5d6a2f` feat + `8df3766` fix(review) | 75/75 tests pass; 100% line coverage; build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (4 MEDIUM + 6 LOW; MEDIUM 1+2+4 applied, MEDIUM 3 hex/binary deferred as YAGNI per MVP; LOW identifier test + column doc applied; CRITICAL infinite-loop bug caught by new exponent test and fixed in the same commit) |
| 5  DbcParser AST+basic  | complete | `9b2c197` feat + fix commit | 115/115 tests pass; 100% line / 98.3% branch coverage on Core; build 0 warn 0 err; code-reviewer WARN (1 HIGH exception-leak + 3 MEDIUM) — all HIGH/MEDIUM fixed: ParseUInt/ParseByte/ParseLong wrap OverflowException, HashSet seenIds replaces O(n²) duplicate check, dead Keyword_SG_ removed from IsStructuralKeyword, trailing newlines applied |
| 6  DBC parser multiplexed M/m + VAL_ | complete | `8eda017` feat + `028ef42` fix(review) | 125/125 tests pass (10 new in DbcParserMultiplexedTests); build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (3 MEDIUM + 6 LOW) — MEDIUM 1 reverted Message.Signals to IReadOnlyList and used 'with' rebuild, MEDIUM 2 added DbcParseException for unknown msg id (including VAL_-before-BO_), MEDIUM 3 added 7 edge-case tests; LOW applied: Peek bounds, m<N> range/malformed error, FindSignalIndex + ReplaceSignalValueTableName helpers; LOW deferred: inline-VAL_ self-table naming (documented MVP shortcut, follow-up), LastOrDefault duplicate-name (rare in real DBC) |
| 7  Core SignalDecoder (LE/BE/signed/float/double) | complete | `cced679` feat + `ff79c74` fix(review) | 137/137 tests pass (12 total SignalDecoderTests: 3 plan + 3 added BE/signed/length>64 + 6 review MEDIUM tests for IEEE 754, len=64 boundary, BE non-zero start, LE byte boundary); build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (2 MEDIUM + 5 LOW) — MEDIUM 1 documented silent short-frame truncation in XML doc, MEDIUM 2 added 6 tests covering Float/Double / signed-16 / BE-non-zero-start / LE-byte-boundary / length=64 boundary; LOW applied: classic-CAN comment fix, IEEE 754 host-endian-agnostic note, SignExtend len=64 comment; LOW deferred: vectorize 8 bits at once (follow-up if WPF trace view shows decoder hot), throw on invalid enum (low value, leave default arm) |
| 8  Infrastructure PeakError + Mapper | complete | `41cb0a1` feat + `bdaee18` fix(review) | 14/14 Infrastructure tests pass (PeakErrorMapperTests: 5 plan InlineData + 3 fact added OK msg / unknown hex / non-empty sweep + 6 review MEDIUM tests for IsOk Theory 4 cases / bitmasked composite / uint.MaxValue); build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (2 MEDIUM + 3 LOW) — MEDIUM 1 documented MVP limitation that bit-flag composites (PCAN_ERROR_INITIALIZE\|BUSOFF) fall through to Unknown, MEDIUM 2 added IsOk helper to remove the OK-trap; LOW applied: 2 boundary tests (composite + max uint); LOW deferred: return type `Error` record (no callers yet, defer to call site materialization), split HardwareBusy into Transient vs Reset (UI message text already distinguishes them) |
| 9  ICanChannel + PeakCanChannel adapter | complete | `ae8240b` feat + `40ca9b0` fix(review) | 167/167 pass + 2 SKIP (integration) (137 Core + 30 Infrastructure PeakCanFrameFormatterTests: 11 DLC table + padding/truncate/empty variants); build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (3 MEDIUM + 7 LOW) — 5 SDK deviations from plan (no TPCANHandle, no TPCANTimestampFD, InitializeFD takes String, no FD_BRS combined, no TPCANMessageId flag consts) — all verified against actual `PCANBasic.NET.dll` v5.0.1.1131; MEDIUM 1 removed dead Channel<CanFrame> field, MEDIUM 2 read-loop backoff 1/10/50ms after consecutive failures, MEDIUM 3 BaudRate redesigned with ClassicCode field + FromDescriptor factory; LOW applied: single-use XML doc, best-effort teardown comment, Unit → own file, IFrameSource future-router note, 18 unit tests for PeakCanFrameFormatter helpers (Span.CopyTo → bounded Buffer.BlockCopy); LOW deferred: expose ReadLoopFaulted event (wait for ChannelRouter), ILogger integration (wait for WPF layer) |
| 10 ChannelRouter fan-out | complete | `822c9c4` feat + `?` fix(review) | 178/178 pass + 2 SKIP (137 Core + 41 Infrastructure ChannelRouterTests: 6 plan tests + 4 null-arg + 1 OnError-throw → auto-detach; plan had 2 tests, implementation added 4 idempotency/isolation/forwarding tests, then 5 more for review fixes); build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (3 MEDIUM + 3 LOW) — MEDIUM 1 OnError secondary throw now logged + auto-detaches sink (spec 6.2 compliance), MEDIUM 2 OCE excluded from catch so sink-shutdown propagates cleanly, MEDIUM 3 per-frame ToArray allocation deferred to follow-up (ImmutableArray or reused buffer); LOW applied: 5 new tests (4 null-arg + auto-detach), XML note on RegisterChannel explaining concrete-only; LOW deferred: per-frame allocation (per MEDIUM 3) |
| 11 BusStatisticsCollector | complete | `0724993` feat + `3c2f1b2` fix(review) | 53/53 pass + 2 SKIP (Infrastructure full suite; 12 new BusStatisticsCollectorTests: 6 plan tests rewritten + 6 added — empty snapshot / BPS / load saturation / load linearity / error-only / window-slide / OnError no-op / concurrent producers / default-struct / record equality). Coverage on `BusStatisticsCollector.cs`: 100% line / 91.66% branch; build 0 warn 0 err; code-reviewer APPROVE_WITH_NOTES (2 MEDIUM + 4 LOW) — MEDIUM 1 OnError no-op replaced with Debug.WriteLine mirroring ChannelRouter.cs:121 (spec 6.2 compliance), MEDIUM 2 Snapshot non-atomic across counter/queue reads fixed by moving counter writes + reads under single `_recentLock` (Interlocked removed — lock provides visibility + atomicity, Snapshot path is not hot); LOW 1 LoadPercent param renamed `framesPerSecond` → `framesInWindow` for self-doc, LOW 2 trim `now` already hoisted (no fix), LOW 3-4 deterministic clock seam deferred to Task 17 / Task 19 per MVP scope |
| 12 AppHostBuilder + AppShell + Connect UI | complete | `352add7` feat + `7c74e5c` fix(review) | 202/202 pass + 4 SKIP (Core 137 + Infrastructure 53 + App 12 new — AppHostBuilderTests 8 + AppShellViewModelTests 4 active + 2 hardware SKIP); build 0 warn 0 err across 6 projects; code-reviewer APPROVE_WITH_NOTES (1 HIGH + 5 MEDIUM + 6 LOW) — HIGH 1 deleted orphan MainWindow.xaml/.xaml.cs (dead code kept by WPF auto-globbing); MEDIUM 1 Log.Logger static assignment acknowledged in XML doc (acceptable for MVP, fix deferred), MEDIUM 2 added `await channel.DisposeAsync()` on failed Connect branch (PeakCanChannel ctor allocates CTS even if SDK never acquired), MEDIUM 3 rewrote threading XML doc to correctly attribute UI-thread affinity to captured DispatcherSynchronizationContext (NOT CommunityToolkit.Mvvm source-gen as previously stated), MEDIUM 4 added STRING-COUPLED comment at CanConnect predicate flagging the probe-output coupling, MEDIUM 5 changed AppShell registration from `AddSingleton<AppShell>()` to factory `sp => new AppShell { DataContext = ... }` so STA-trap is opt-in for callers (production: App.OnStartup qualifies); LOW applied: empty `<Application.Resources>` kept (cosmetic), PcanUsbFdFirstHandle duplication kept with cross-reference comments (Task 13 may extract to Core), `result.Error!` kept (acceptable), `using Microsoft.Extensions.Hosting;` style deferred, stub sealed-class kept (Tasks 13-17 replace wholesale), added 1 new CanExecute=true test; LOW deferred: stub-to-interface refactor (Tasks 13-17 wholesale replace), DI lifetime changes (single-window MVP) |
| 13 Trace view        | complete | `e56b024` feat + `a8c1a3b` fix(review) | 214/214 pass + 4 SKIP (Core 137 + Infrastructure 53 + App 24 — +12 new App tests this task: TraceViewModelTests 4 + TraceServiceTests 6 incl. DroppedFrames counter + SinkWiringServiceTests 2; AppShellViewModel NewVm helper updated for TraceViewModel ctor param); build 0 warn 0 err across 6 projects; code-reviewer APPROVE_WITH_NOTES (1 HIGH + 4 MEDIUM + 6 LOW) — HIGH 1 `AppendBatch_Adds_All_Frames_As_TraceEntries` test name lied (only asserted IsCompletedSuccessfully) → renamed to `AppendBatch_With_Null_Application_Returns_CompletedTask_Without_Adding` (truthful), and added new STA `[Fact]` `AppendBatch_On_StaThread_With_Application_Adds_All_Frames` that creates WPF `Application` on a dedicated STA thread, invokes AppendBatchAsync, and pumps dispatcher via `DispatcherFrame` until the awaited task completes (asserts `Entries.Count == 3`); MEDIUM 1 added `_droppedOnFullChannel` Interlocked counter + Debug.WriteLine every 100 drops to satisfy spec §6.2 "dropped frames only log"; MEDIUM 2 `internal sealed SinkWiringService` + `InternalsVisibleTo` accepted as MVP cost (Task 18 NetArchTest may scope tighter); MEDIUM 3 replaced `Task.Delay(50, ct)` + try/catch with `PeriodicTimer.WaitForNextTickAsync(stoppingToken)` (cleaner intent, removed manual cancellation trap); MEDIUM 4 STA test added — covers the production dispatcher path that was previously uncovered; LOW 1 Channel naming consistency deferred (single-channel MVP); LOW 2 List<CanFrame> grow acceptable; LOW 3 StopAsync symmetry acceptable (shared lifetime); LOW 4 renamed `StartAsync_Is_Idempotent_On_Second_Call` → `StartAsync_Second_Call_Is_Safe_Via_AttachSink_Idempotency`; LOW 5 FIFO trim O(N) acceptable for MVP burst rate |
| 14 Send view        | complete | `ce64e3f` feat + `53c4d38` fix(review) | 235/235 pass + 5 SKIP (Core 137 + Infrastructure 53 + App 45 — +21 new App tests this task: SendServiceTests 5 + SendViewModelTests 15 + AppShellViewModelTests +1 hardware SKIP); build 0 warn 0 err across 6 projects; code-reviewer APPROVE_WITH_NOTES (1 HIGH + 5 MEDIUM + 6 LOW) — HIGH 1 replaced SendService.ActiveChannel plain setter with `Interlocked.Exchange` + `Volatile.Read` for thread-safe setter (was MVP single-thread; now safe for background-thread callers like Task 17 stats pump); MEDIUM 1 added ParseHex empty-input guard — `if (stripped.Length == 0) throw new FormatException("Hex data is empty")` — and surfaced via existing `catch (FormatException)` branch (was: silent DLC=0 transmission, footgun); MEDIUM 2 added ID pre-validation against chosen FrameFormat (`raw > maxId → friendly status`) instead of letting CanId ctor throw opaque ArgumentOutOfRangeException; MEDIUM 3 ConnectAsync success-branch reordered to set `IsConnected = true` BEFORE `_sendService.ActiveChannel = channel` so no window where Send can fire against a channel the UI still considers disconnected (XML doc added); MEDIUM 4 verified `LogSendThrew` already called in catch block (reviewer misread — no fix); LOW 1 `SendService.SendAsync` `virtual` justified by FakeSendService override (acceptable); LOW 2 `[RelayCommand] private SendAsync` is standard MVVM Toolkit pattern (acceptable); LOW 3 SendButton CanExecute deferred to Task 15 (shell wiring); LOW 4 added 5 new tests for hex parser edge cases (lowercase, leading whitespace, all-separators) + ID range validation (Standard 11-bit overflow → friendly status; Extended 29-bit acceptance) — fixed existing `SendCommand_With_Extended_Id_Uses_Extended_FrameFormat` test that was using empty DataText (broke under MEDIUM-1 fix) |
| 15 DBC view + shell nav | complete | `7c916bc` feat + `1429a09` fix(review) | 251/251 pass + 6 SKIP (Core 137 + Infrastructure 53 + App 61 — +16 new App tests this task: DbcServiceTests 5 + DbcMessageViewModelTests 3 + DbcViewModelTests 4 active + 1 STA-blocked SKIP + AppShellViewModelTests delta -1 stub test deleted +4 view-switching; AppShellViewModel NewVm helper updated for DbcViewModel + SendViewModel ctor params + FakeDbcService); build 0 warn 0 err across 6 projects; code-reviewer WARN (2 HIGH + 1 MEDIUM + 3 LOW) — HIGH 1 DbcViewModel.OnLoaded/OnLoadFailed now marshal via `Application.Current.Dispatcher.Invoke` (cross-thread ObservableCollection mutation would throw NotSupportedException — production path fixes this; test path falls back to inline since Application.Current is null); HIGH 2 dropped IDisposable on DbcViewModel (singleton in DI — Dispose never called, was latent footgun at shutdown; documented in XML that DbcService + DbcViewModel die together at process exit); MEDIUM reviewer's suggestion to drop `partial` from DbcService was REJECTED — WPF source-gen auto-emits a partial declaration (CS0260 verified on attempt to drop); kept `partial` and removed `sealed` since `LoadAsync` is `virtual` for FakeDbcService override; LOW 1 ctor 6-arg explosion deferred (Tasks 16/17 add 2 more VMs); LOW 2 Show seed consistency deferred (production flow guarantees order); LOW 3 OpenAsync cancel test SKIPPED via `[Fact(Skip = "WPF OpenFileDialog.ShowDialog blocks on non-STA without a message pump; covered by Task 19/20 smoke + the 4 event-driven tests cover the load-result VM transitions")]` — the previous try/catch did not protect against blocking calls (only thrown exceptions); test preserved for future IFileDialogService refactor |
| 16 Signal view        | complete | `b001484` feat + `4da6cc9` fix | 266/266 pass + 6 SKIP (Core 137 + Infrastructure 53 + App 76 — +15 new App tests this task: SignalViewModelTests 8 + TraceServiceTests +3 DBC-decode + DbcViewModelTests +1 reset-on-load + AppShellViewModelTests +3 ShowSignals/cached/OpenDbc-no-reset; AppShellViewModel ctor 6→7 args for SignalViewModel; SignalViewModel added as DI singleton in AppHostBuilder line 73); build 0 warn 0 err across 6 projects; **plan deviations**: (a) dropped `_byKey` dead field from `SignalViewModel.ApplyFrame` (never read), (b) **fix 4da6cc9**: original implementation mutated `Latest` directly on the SDK read thread — subagent's claim "WPF DataGrid binding internally marshals CollectionChanged" was incorrect (WPF throws NotSupportedException on cross-thread `ItemsControl` mutation). Applied the Task 15 DbcViewModel dispatcher pattern: decode on calling thread (pure), build a local `List<SignalEntry>`, hop to UI thread via `Dispatcher.InvokeAsync` (non-blocking so SDK thread not slowed at ~8 kfps). Test context (no Application) falls back to inline. A dedicated STA test was tried + rolled back — it caused xunit parallel-execution hangs in the App.Tests suite. (c) AppShellViewModel ctor explosion flagged as LOW follow-up for AppDependencies refactor in Task 17 wrap-up; no code-reviewer subagent dispatched this run (single-developer, TDD self-validated; fix caught + applied by main agent after subagent handoff) |
| 17 Stats view         | complete | `278d839` | 281/281 pass + 6 SKIP (Core 137 + Infrastructure 53 + App 91 — +15 new App tests: StatsViewModelTests 7 + StatisticsServiceTests 6 + AppShellViewModelTests +2 ShowStats; AppShellViewModel ctor 7→8 args for StatsViewModel; StatisticsService + StatsViewModel already in AppHostBuilder); build 0 warn 0 err across 6 projects; **plan deviations**: (a) LiveChartsCore 2.0.4 → OxyPlot.Wpf 2.2.0 rewrite (Sprint 0 deviation; LiveCharts native deps are .NET Framework only), (b) `StatsViewModel` pre-fills the rolling window with 60 zeros for a stable chart axis range before the first 1 Hz tick, (c) `StatisticsService` takes the first snapshot immediately (no 1 s blank window), (d) `AppShellViewModel` ctor 8-arg explosion flagged for AppDependencies refactor in Task 18 wrap-up |
| 18 NetArchTest        | complete | `b9ea820` | 5/5 rules pass + 281/281 unit tests + 6 SKIP (Core 137 + Infrastructure 60 + 2 SKIP [was 53; +5 NetArchTest rules, +2 hardware SKIP not changing] + App 91); build 0 warn 0 err across 6 projects; **refactor**: rule 3 (`App_Should_Not_Depend_On_Peak_Can_Basic`) caught `AppShellViewModel` directly using `PCANBasic.Initialize/Uninitialize` + `TPCANBaudrate` — extracted to `IChannelProbe` (Core) + `PeakChannelProbe` (Infrastructure.Peak); `AppShellViewModel` ctor 8→9 args, 2 unused logger methods dropped, `using Peak.Can.Basic.BackwardCompatibility` removed; `AppHostBuilder` registers `IChannelProbe`; test project `FakeChannelProbe` always returns Ok; **plan deviations**: (a) `Infrastructure.Tests` TFM `net10.0` → `net10.0-windows` (required to reference App assembly; plan §18 did not anticipate the TFM conflict — verified by NU1201 build error), (b) explicit `AddSingleton<IChannelProbe, PeakChannelProbe>()` registration (plan §18 implicit) |

## Resume Guide (next session)

To continue:
1. Read this ledger and `docs/superpowers/plans/2026-06-18-peakcan-host-implementation.md` once.
2. `cd D:/claude_proj2/peakcan-host && git log --oneline` to confirm baseline.
3. Continue at **Task 17** (Stats view — OxyPlot). From here on, ALL App-layer tasks run serially because they share AppShell.xaml + DI registrations.
4. Per the SDD skill, dispatch a fresh subagent per task, then a task-reviewer subagent; mark each complete + append to this ledger only after the review verdict is clean.
5. Plan §17 must use OxyPlot.Wpf, not LiveChartsCore (deviation). StatsViewModel/StatsView code blocks reference `LiveChartsCore` types — rewrite using `OxyPlot.PlotModel` + `LineSeries` before implementing Task 17. **Status: done (Task 17 commit `278d839`).**
6. Current test count: **281 pass + 6 SKIP** (Core 137 + Infrastructure 60 + App 91 — Infrastructure +7 from 5 NetArchTest rules + 2 hardware SKIP; App +15 from Stats view). Test project `tests/PeakCan.Host.App.Tests/` targets `net10.0-windows` with `<UseWPF>true</UseWPF>`; `tests/PeakCan.Host.Infrastructure.Tests/` TFM `net10.0` → `net10.0-windows` to allow App reference (Task 18).
6a. xunit parallel-execution flake: `TraceServiceTests.ExecuteAsync_Periodically_Flushes_Channel_Into_VM_Batch` hangs when all App.Tests run in parallel; passes individually in 241 ms. The test creates a `BackgroundService` whose `ExecuteAsync` runs on a hosted-service thread — under heavy parallel load the 5 s cts timeout expires before the `PeriodicTimer` 50 ms tick has drained. Task 19 (CI) should add `[CollectionDefinition("AppTests", DisableParallelization = true)]` or run App.Tests in a single-assembly mode. Per-test timeout bump to 30 s is also an option.
7. Task 15 ship verification: lazy view init pattern (ShowTraceCommand from AppShell.xaml.cs SourceInitialized) — the next view-bearing task should follow the same pattern (CacheNull → ShowX lazily constructs → bind to CurrentView). Add `RunSta` helper to any test that instantiates WPF UserControls.

## Session boundary

Last session reached the implementer-parallel decision and ran Task 1 manually (scaffolding + restore + build + commit). User opted to stop at Task 1 rather than risk mid-batch crash from context exhaustion.
| 2  Core domain types  | complete | `9ee928d` | 11/11 tests pass; 0 warn 0 err build |
| 3  Result<T>          | complete | `8ffdb29` | 32/32 tests pass; 100% coverage |
| 4  DBC Tokenizer      | complete | `f5d6a2f` + `8df3766` | 75/75 tests pass; 100% line |
| 5  DBC parser 基础    | complete | `9b2c197` + `cac575e` | 115/115 tests pass; 100% line / 98.3% branch |
| 6  DBC parser 高级    | complete | `8eda017` + `028ef42` | 125/125 tests pass (10 new); review APPROVE_WITH_NOTES → all MEDIUM fixed |
| 7  SignalDecoder      | complete | `cced679` + `ff79c74` | 137/137 tests pass; 100% line on SignalDecoder |
| 8  PeakError + Mapper | complete | `41cb0a1` + `?` | 14/14 tests pass |
| 9  ICanChannel        | complete | `?` + `?` | 167/167 + 2 SKIP; 18 new unit tests for PeakCanFrameFormatter |
| 10 ChannelRouter      | complete | `822c9c4` + `?` | 178/178 + 2 SKIP; 11 ChannelRouter tests |
| 11 BusStatisticsCollector | complete | `0724993` + `3c2f1b2` | 53/53 Infrastructure + 2 SKIP; 12 BusStatisticsCollector tests; review APPROVE_WITH_NOTES → 2 MEDIUM + 4 LOW all closed |
| 12 AppShell + Connect UI | complete | `352add7` + `7c74e5c` | 12/12 App + 2 SKIP; review APPROVE_WITH_NOTES → 1 HIGH + 5 MEDIUM + 6 LOW all closed |
| 13 Trace view         | complete | `e56b024` + `a8c1a3b` | 24/24 App + 2 SKIP; review APPROVE_WITH_NOTES → 1 HIGH + 4 MEDIUM + 6 LOW all closed |
| 14 Send               | complete | `ce64e3f` + `53c4d38` | 45/45 App + 3 SKIP; review APPROVE_WITH_NOTES → 1 HIGH + 5 MEDIUM + 6 LOW all closed |
| 15 DBC view + shell nav | complete | `7c916bc` + `1429a09` | 61/61 App + 4 SKIP; review WARN → 2 HIGH + 1 MEDIUM + 3 LOW closed |
| 16 Signal view        | complete | `b001484` + `4da6cc9` | 266/266 pass + 6 SKIP; dispatcher marshaling fix post-subagent handoff |
| 17 Stats view         | complete | `278d839` | 281/281 pass + 6 SKIP; OxyPlot rewrite |
| 18 NetArchTest        | complete | `b9ea820` | 5/5 rules + IChannelProbe refactor |
| 19 CI + coverage      | pending | — | — |
| 20 Final smoke + publish | pending | — | — |
| 21 README             | pending | — | — |

## Conventions
- One task = one commit (or small commit chain within a single task).
- Implementer dispatches use `claude` general-purpose subagent.
- Reviewer dispatches use `claude` general-purpose subagent.
- Model: implementer + reviewer use `claude` (session default) for now; can be downgraded per task to save cost.
- Tests required to pass before commit; coverage thresholds enforced at Task 19.

## Notes
- Wave 1 must complete before Wave 2; Wave 2 before Wave 3; Wave 3 before Wave 4; Wave 4 before Wave 5; Wave 5/6/7 in any order after.
- Hardware-dependent verification (Task 9 `PeakCanChannel` integration test) is `[Trait("category","integration")]` and skipped in CI.
- LiveCharts2 2.0.0-rc5.4 is a pre-release; if it breaks, fall back to OxyPlot.Wpf.
