<Project>
  <PropertyGroup>
    <!-- v3.13.3 PATCH F6: window title version. Generated by Tier 3
         ship script. Manual edits get overwritten on next ship.
         Placed in src/ so it applies to App + Infrastructure but NOT
         to test projects (tests don't ship a UI). Overrides the
         repo-root Directory.Build.props Version=2.0.0 default for
         shipping artifacts only. -->
<Version>3.57.0</Version>
    <AssemblyVersion>3.57.0.0</AssemblyVersion>
    <FileVersion>3.57.0.0</FileVersion>
    <InformationalVersion>3.57.0</InformationalVersion>
  </PropertyGroup>
</Project>
