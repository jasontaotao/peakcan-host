<Window x:Class="PeakCan.Host.App.Windows.ConnectionSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PeakCan.Host.App.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance Type=vm:ConnectionSettingsViewModel}"
        mc:Ignorable="d"
        Title="连接设置" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <StackPanel Margin="18">
        <TextBlock Text="连接设置" FontWeight="Bold" FontSize="15" Margin="0,0,0,14" />
        <TextBlock Text="参数由所选设备自动提供——换 CAN 盒时此面板自动变成对应参数。"
                   Foreground="{StaticResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12" />

        <!-- 设备类型 -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="96"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="设备类型" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding Devices}"
                      SelectedItem="{Binding SelectedDevice}" DisplayMemberPath="DisplayName" />
        </Grid>

        <!-- 通道 -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="96"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="通道" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding Channels}" SelectedItem="{Binding SelectedChannel}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding Name}" />
                            <Run Text=" (0x" /><Run Text="{Binding Handle, StringFormat=X2}" /><Run Text=")" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <!-- CAN 模式 -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="96"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="CAN 模式" VerticalAlignment="Center"/>
            <CheckBox Grid.Column="1" Content="启用 CAN FD" IsChecked="{Binding IsFd}" VerticalAlignment="Center" />
        </Grid>

        <!-- 波特率 / FD 数据段速率 -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="96"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="{Binding RateLabel}" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableBaudRates}"
                      SelectedItem="{Binding SelectedBaudRate}" DisplayMemberPath="Name" />
        </Grid>

        <TextBlock Text="标准帧 / 扩展帧是每帧属性，发送时选择，不在此配置。"
                   Foreground="{StaticResource TextSecondary}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,16" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="应用并连接" Command="{Binding ApplyAndConnectCommand}"
                    Padding="14,5" Margin="0,0,8,0" IsDefault="True" />
            <Button Content="取消" Click="OnCancelClick" Padding="14,5" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
