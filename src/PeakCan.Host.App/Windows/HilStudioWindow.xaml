<Window x:Class="PeakCan.Host.App.Windows.HilStudioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:PeakCan.Host.App.Controls"
        xmlns:e="clr-namespace:PeakCan.Host.App.ViewModels.EcuSimulator"
        Title="HIL Configuration Studio" Width="1400" Height="800"
        WindowStartupLocation="CenterOwner">
  <Window.Resources>
    <controls:EcuResponseModeToVisibilityConverter x:Key="EcuResponseModeToVisibilityConverter"/>
    <!-- ===== 12 个 step kind 的属性面板模板（只实例化激活的） ===== -->
    <DataTemplate x:Key="SendFrameTemplate">
      <StackPanel>
        <TextBlock Text="CAN ID" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcMessages, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="Hex"
                  SelectedValue="{Binding Params[Id], Mode=TwoWay}"/>
        <CheckBox Content="CAN FD" IsChecked="{Binding Params[Fd], Mode=TwoWay}" Margin="0,4,0,0"/>
        <CheckBox Content="Extended ID" IsChecked="{Binding Params[Extended], Mode=TwoWay}" Margin="0,2,0,0"/>
        <TextBlock Text="Composer (pick message → fill signals)" FontWeight="SemiBold" Margin="0,10,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.Composer.DbcMessages, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display"
                  SelectedItem="{Binding DataContext.SuiteBuilder.Composer.SelectedMessage, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay}"/>
        <ItemsControl ItemsSource="{Binding DataContext.SuiteBuilder.Composer.SignalValues, RelativeSource={RelativeSource AncestorType=Window}}"
                      Margin="0,4,0,0">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal" Margin="0,1">
                <TextBlock Text="{Binding Name}" Width="100"/>
                <TextBox Text="{Binding ValueText, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
          <Button Content="Compose Data" Command="{Binding DataContext.SuiteBuilder.ComposeDataCommand, RelativeSource={RelativeSource AncestorType=Window}}" Padding="6,1"/>
          <TextBlock Text="Data:" VerticalAlignment="Center" Margin="8,0,4,0"/>
          <TextBox Text="{Binding Params[Data], Mode=TwoWay}" Width="120" FontFamily="Consolas"/>
        </StackPanel>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="ExpectFrameTemplate">
      <StackPanel>
        <TextBlock Text="CAN ID" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcMessages, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="Hex"
                  SelectedValue="{Binding Params[Id], Mode=TwoWay}"/>
        <CheckBox Content="Extended ID" IsChecked="{Binding Params[Extended], Mode=TwoWay}" Margin="0,4,0,0"/>
        <TextBlock Text="Data mask (hex, optional)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[DataMask], Mode=TwoWay}" FontFamily="Consolas"/>
        <TextBlock Text="Timeout (ms)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[TimeoutMs], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="WaitForSignalTemplate">
      <StackPanel>
        <TextBlock Text="Signal" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcSignals, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="FullName"
                  IsTextSearchEnabled="True" TextSearch.TextPath="FullName"
                  SelectedValue="{Binding Params[SignalName], Mode=TwoWay}"/>
        <TextBlock Text="Expected" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[Expected], Mode=TwoWay}"/>
        <TextBlock Text="Tolerance" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[Tolerance], Mode=TwoWay}"/>
        <TextBlock Text="Timeout (ms)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[TimeoutMs], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="AssertSignalTemplate">
      <StackPanel>
        <TextBlock Text="Signal" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcSignals, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="FullName"
                  IsTextSearchEnabled="True" TextSearch.TextPath="FullName"
                  SelectedValue="{Binding Params[SignalName], Mode=TwoWay}"/>
        <TextBlock Text="Expected" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[Expected], Mode=TwoWay}"/>
        <TextBlock Text="Tolerance" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[Tolerance], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="AssertRangeTemplate">
      <StackPanel>
        <TextBlock Text="Signal" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcSignals, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="FullName"
                  IsTextSearchEnabled="True" TextSearch.TextPath="FullName"
                  SelectedValue="{Binding Params[SignalName], Mode=TwoWay}"/>
        <TextBlock Text="Min" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[Min], Mode=TwoWay}"/>
        <TextBlock Text="Max" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[Max], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="AssertResponseTimeTemplate">
      <StackPanel>
        <TextBlock Text="Request ID" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcMessages, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="Hex"
                  SelectedValue="{Binding Params[ReqId], Mode=TwoWay}"/>
        <TextBlock Text="Response ID" Margin="0,6,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcMessages, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="Hex"
                  SelectedValue="{Binding Params[RespId], Mode=TwoWay}"/>
        <CheckBox Content="Req extended" IsChecked="{Binding Params[ReqExtended], Mode=TwoWay}" Margin="0,4,0,0"/>
        <CheckBox Content="Resp extended" IsChecked="{Binding Params[RespExtended], Mode=TwoWay}" Margin="0,4,0,0"/>
        <TextBlock Text="Max (ms)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[MaxMs], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="AssertDtcTemplate">
      <StackPanel>
        <TextBlock Text="DTC (hex, optional)" Margin="0,0,0,2"/>
        <TextBox Text="{Binding Params[DtcCode], Mode=TwoWay}"/>
        <CheckBox Content="Expect present" IsChecked="{Binding Params[ExpectPresent], Mode=TwoWay}" Margin="0,6,0,0"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="AssertNrcTemplate">
      <StackPanel>
        <TextBlock Text="Service ID (hex)" Margin="0,0,0,2"/>
        <TextBox Text="{Binding Params[ServiceId], Mode=TwoWay}"/>
        <TextBlock Text="Expected NRC (hex)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[ExpectedNrc], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="DelayTemplate">
      <StackPanel>
        <TextBlock Text="Delay (ms)" Margin="0,0,0,2"/>
        <TextBox Text="{Binding Params[Milliseconds], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="CommentTemplate">
      <StackPanel>
        <TextBlock Text="Comment" Margin="0,0,0,2"/>
        <TextBox Text="{Binding Params[Text], Mode=TwoWay}" AcceptsReturn="True" Height="60" TextWrapping="Wrap"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="InjectFaultTemplate">
      <StackPanel>
        <TextBlock Text="CAN ID" Margin="0,0,0,2"/>
        <ComboBox ItemsSource="{Binding DataContext.SuiteBuilder.DbcMessages, RelativeSource={RelativeSource AncestorType=Window}}"
                  DisplayMemberPath="Display" SelectedValuePath="Hex"
                  SelectedValue="{Binding Params[CanId], Mode=TwoWay}"/>
        <TextBlock Text="Fault type" Margin="0,6,0,2"/>
        <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding Params[FaultType], Mode=TwoWay}">
          <ComboBoxItem Content="Drop" Tag="Drop"/>
          <ComboBoxItem Content="Delay" Tag="Delay"/>
          <ComboBoxItem Content="Corrupt" Tag="Corrupt"/>
          <ComboBoxItem Content="Duplicate" Tag="Duplicate"/>
        </ComboBox>
        <TextBlock Text="Probability (0-1)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[Probability], Mode=TwoWay}"/>
        <TextBlock Text="Delay (ms)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[DelayMs], Mode=TwoWay}"/>
        <TextBlock Text="Corrupt XOR mask (hex)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[CorruptXorMask], Mode=TwoWay}" FontFamily="Consolas"/>
        <TextBlock Text="Fault ID (optional)" Margin="0,6,0,2"/>
        <TextBox Text="{Binding Params[FaultId], Mode=TwoWay}"/>
        <TextBlock Text="Direction" Margin="0,6,0,2"/>
        <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding Params[Direction], Mode=TwoWay}">
          <ComboBoxItem Content="Send" Tag="Send"/>
          <ComboBoxItem Content="Receive" Tag="Receive"/>
          <ComboBoxItem Content="Both" Tag="Both"/>
        </ComboBox>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="ClearFaultTemplate">
      <StackPanel>
        <TextBlock Text="Fault ID (empty = clear all)" Margin="0,0,0,2"/>
        <TextBox Text="{Binding Params[FaultId], Mode=TwoWay}"/>
      </StackPanel>
    </DataTemplate>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" MinWidth="320"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*" MinWidth="240"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*" MinWidth="340"/>
    </Grid.ColumnDefinitions>

    <!-- ===== col 0: DBC Browser（Phase 1） ===== -->
    <Grid Grid.Column="0" Margin="8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*" MinHeight="120"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*" MinHeight="120"/>
      </Grid.RowDefinitions>

      <StackPanel Orientation="Horizontal">
        <Button Content="Open DBC..." Command="{Binding OpenCommand}" Padding="8,2"/>
        <TextBlock Text="{Binding Status}" Margin="8,3,0,0" VerticalAlignment="Center"/>
      </StackPanel>

      <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,6,0,4">
        <TextBlock Text="Search:" VerticalAlignment="Center"/>
        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="4,0"/>
        <TextBlock Text="{Binding FilteredMessages.Count, StringFormat='{}{0} messages'}"
                   VerticalAlignment="Center" Margin="4,0,0,0" Foreground="Gray"/>
      </StackPanel>

      <DataGrid Grid.Row="2" ItemsSource="{Binding FilteredMessages}"
                IsReadOnly="True" EnableRowVirtualization="True" RowHeight="20"
                AlternatingRowBackground="#F8F8F8"
                SelectedItem="{Binding SelectedMessage, Mode=TwoWay}"
                AutoGenerateColumns="False">
        <DataGrid.Columns>
          <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="110"/>
          <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
          <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="50"/>
          <DataGridTextColumn Header="Sender" Binding="{Binding Sender}" Width="110"/>
          <DataGridTextColumn Header="Signals" Binding="{Binding SignalCount}" Width="70"/>
          <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="*">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="Gray"/>
                <Setter Property="FontStyle" Value="Italic"/>
              </Style>
            </DataGridTextColumn.ElementStyle>
          </DataGridTextColumn>
        </DataGrid.Columns>
      </DataGrid>

      <GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" Background="#CCCCCC"
                    ResizeBehavior="PreviousAndNext"/>

      <Grid Grid.Row="4">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding SelectedMessage.Name, StringFormat='Signals of {0}'}"
                   FontWeight="SemiBold" Margin="0,2,0,2"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding VisibleSignals}"
                  IsReadOnly="True" EnableRowVirtualization="True" RowHeight="20"
                  AlternatingRowBackground="#F8F8F8"
                  RowDetailsVisibilityMode="VisibleWhenSelected"
                  SelectedItem="{Binding SelectedSignal, Mode=TwoWay}"
                  AutoGenerateColumns="False">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
            <DataGridTextColumn Header="Bits" Binding="{Binding BitLayout}" Width="110"/>
            <DataGridTextColumn Header="Scale" Binding="{Binding FactorOffset}" Width="90"/>
            <DataGridTextColumn Header="Range" Binding="{Binding MinMax}" Width="110"/>
            <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="70"/>
            <DataGridTextColumn Header="Value Table" Binding="{Binding ValueTableName}" Width="110"/>
            <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="*">
              <DataGridTextColumn.ElementStyle>
                <Style TargetType="TextBlock">
                  <Setter Property="Foreground" Value="Gray"/>
                  <Setter Property="FontStyle" Value="Italic"/>
                </Style>
              </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
          </DataGrid.Columns>
          <DataGrid.RowDetailsTemplate>
            <DataTemplate>
              <Border Visibility="{Binding ValueTableEntries,
                          Converter={StaticResource NullToVisibilityConverter}}"
                      Margin="16,4,4,4">
                <StackPanel>
                  <TextBlock Text="{Binding ValueTableName, StringFormat='Value Table: {0}'}"
                             FontWeight="SemiBold" FontSize="11"/>
                  <ItemsControl ItemsSource="{Binding ValueTableEntries}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,1">
                          <TextBlock Text="{Binding Key}" FontFamily="Consolas" Foreground="Gray" Width="48"/>
                          <TextBlock Text=" = "/>
                          <TextBlock Text="{Binding Label}"/>
                        </StackPanel>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </Border>
            </DataTemplate>
          </DataGrid.RowDetailsTemplate>
        </DataGrid>
      </Grid>
    </Grid>

    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" Background="#CCCCCC"
                  ResizeBehavior="PreviousAndNext"/>

    <!-- ===== col 2: Test Suite Builder（Phase 2） ===== -->
    <Grid Grid.Column="2" Margin="8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <StackPanel Orientation="Horizontal">
        <Button Content="Open" Command="{Binding SuiteBuilder.OpenCommand}" Padding="6,1"/>
        <Button Content="Save" Command="{Binding SuiteBuilder.SaveCommand}" Padding="6,1"/>
        <Button Content="SaveAs" Command="{Binding SuiteBuilder.SaveAsCommand}" Padding="6,1"/>
        <TextBlock Text="Suite:" VerticalAlignment="Center" Margin="6,0,2,0"/>
        <TextBox Text="{Binding SuiteBuilder.SuiteName}" Width="90"/>
        <Button Content="+Case" Command="{Binding SuiteBuilder.AddCaseCommand}" Padding="6,1" Margin="6,0,0,0"/>
        <Button Content="-Case" Command="{Binding SuiteBuilder.RemoveCaseCommand}" Padding="6,1"/>
        <TextBlock Text="{Binding SuiteBuilder.Status}" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="Gray"/>
        <TextBlock Text="{Binding SuiteBuilder.ErrorMessage}" VerticalAlignment="Center" Margin="6,0,0,0" Foreground="Firebrick"/>
      </StackPanel>

      <Grid Grid.Row="1" Margin="0,6,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="170"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <TextBlock Text="Test Cases" FontWeight="SemiBold"/>
          <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,2,0,0">
            <TextBlock Text="Name:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding SuiteBuilder.SelectedCase.Name, UpdateSourceTrigger=PropertyChanged}"
                     Width="110" Margin="4,0,0,0"/>
          </StackPanel>
          <ListBox Grid.Row="2" ItemsSource="{Binding SuiteBuilder.Cases}"
                   SelectedItem="{Binding SuiteBuilder.SelectedCase}"
                   DisplayMemberPath="Name"/>
          <TextBlock Grid.Row="3" Text="Toolbox" FontWeight="SemiBold" Margin="0,10,0,2"/>
          <ItemsControl Grid.Row="4" ItemsSource="{Binding SuiteBuilder.AvailableKinds}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Content="{Binding}" Margin="0,1" HorizontalAlignment="Stretch" Padding="4,1"
                        Command="{Binding DataContext.SuiteBuilder.AddStepCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}"/>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" Background="#CCCCCC"
                      ResizeBehavior="PreviousAndNext"/>

        <Grid Grid.Column="2">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Steps" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Content="Remove" Command="{Binding SuiteBuilder.RemoveStepCommand}" Margin="8,0,0,0" Padding="4,0"/>
            <Button Content="▲" Command="{Binding SuiteBuilder.MoveStepUpCommand}" Padding="4,0"/>
            <Button Content="▼" Command="{Binding SuiteBuilder.MoveStepDownCommand}" Padding="4,0"/>
          </StackPanel>
          <ListBox Grid.Row="1" ItemsSource="{Binding SuiteBuilder.SelectedCase.Steps}"
                   SelectedItem="{Binding SuiteBuilder.SelectedStep}">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Kind}" FontWeight="SemiBold"/>
                  <TextBlock Text="{Binding Label, StringFormat='  {0}'}" Foreground="Gray"/>
                </StackPanel>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
          <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch" Background="#CCCCCC"
                        ResizeBehavior="PreviousAndNext"/>
          <!-- 属性面板: 只实例化激活 kind 的模板（修复切换慢） -->
          <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,6,0,0">
            <ContentControl Content="{Binding SuiteBuilder.SelectedStep}">
              <ContentControl.Style>
                <Style TargetType="ContentControl">
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="SendFrame">
                      <Setter Property="ContentTemplate" Value="{StaticResource SendFrameTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="WaitForFrame">
                      <Setter Property="ContentTemplate" Value="{StaticResource ExpectFrameTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="WaitForSignal">
                      <Setter Property="ContentTemplate" Value="{StaticResource WaitForSignalTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="AssertSignal">
                      <Setter Property="ContentTemplate" Value="{StaticResource AssertSignalTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="AssertRange">
                      <Setter Property="ContentTemplate" Value="{StaticResource AssertRangeTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="AssertResponseTime">
                      <Setter Property="ContentTemplate" Value="{StaticResource AssertResponseTimeTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="AssertDtc">
                      <Setter Property="ContentTemplate" Value="{StaticResource AssertDtcTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="AssertNrc">
                      <Setter Property="ContentTemplate" Value="{StaticResource AssertNrcTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="Delay">
                      <Setter Property="ContentTemplate" Value="{StaticResource DelayTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="Comment">
                      <Setter Property="ContentTemplate" Value="{StaticResource CommentTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="InjectFault">
                      <Setter Property="ContentTemplate" Value="{StaticResource InjectFaultTemplate}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding SuiteBuilder.SelectedStep.Kind}" Value="ClearFault">
                      <Setter Property="ContentTemplate" Value="{StaticResource ClearFaultTemplate}"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </ContentControl.Style>
            </ContentControl>
          </ScrollViewer>
        </Grid>
      </Grid>
    </Grid>

    <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" Background="#CCCCCC"
                  ResizeBehavior="PreviousAndNext"/>

    <!-- ===== col 4: ECU Simulator（Phase 3） ===== -->
    <Grid Grid.Column="4" Margin="8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*" MinHeight="150"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*" MinHeight="120"/>
      </Grid.RowDefinitions>

      <!-- 工具栏 -->
      <StackPanel Orientation="Horizontal">
        <Button Content="Open" Command="{Binding EcuSimulator.OpenCommand}" Padding="6,1"/>
        <Button Content="Save" Command="{Binding EcuSimulator.SaveCommand}" Padding="6,1"/>
        <Button Content="SaveAs" Command="{Binding EcuSimulator.SaveAsCommand}" Padding="6,1"/>
        <Button Content="Import ODX" Command="{Binding EcuSimulator.ImportOdxCommand}" Padding="6,1"/>
      </StackPanel>

      <!-- ODX 导入参数 -->
      <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
        <TextBlock Text="ECU:" VerticalAlignment="Center"/>
        <TextBox Text="{Binding EcuSimulator.OdxEcuName, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="4,0,0,0"/>
        <TextBlock Text="Req:" VerticalAlignment="Center" Margin="6,0,0,0"/>
        <TextBox Text="{Binding EcuSimulator.OdxRequestIdHex, UpdateSourceTrigger=PropertyChanged}" Width="52" FontFamily="Consolas" Margin="4,0,0,0"/>
        <TextBlock Text="Resp:" VerticalAlignment="Center" Margin="6,0,0,0"/>
        <TextBox Text="{Binding EcuSimulator.OdxResponseIdHex, UpdateSourceTrigger=PropertyChanged}" Width="52" FontFamily="Consolas" Margin="4,0,0,0"/>
      </StackPanel>

      <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,2,0,0">
        <TextBlock Text="{Binding EcuSimulator.StatusMessage}" Foreground="Gray"/>
        <TextBlock Text="{Binding EcuSimulator.ErrorMessage}" Foreground="Firebrick" Margin="8,0,0,0"
                   TextTrimming="CharacterEllipsis" MaxWidth="320"/>
      </StackPanel>

      <!-- 脚本属性表单 -->
      <Grid Grid.Row="3" Margin="0,6,0,2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" Text="{Binding EcuSimulator.Script.Name, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="1" Text="Initial state:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EcuSimulator.Script.InitialState, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Column="2" Text="ReqID:" VerticalAlignment="Center" Margin="8,0,0,0"/>
        <TextBox Grid.Column="3" Text="{Binding EcuSimulator.Script.RequestIdHex, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
        <TextBlock Grid.Row="1" Grid.Column="2" Text="RespID:" VerticalAlignment="Center" Margin="8,0,0,0"/>
        <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding EcuSimulator.Script.ResponseIdHex, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
        <CheckBox Grid.Row="1" Grid.Column="3" Content="Extended" IsChecked="{Binding EcuSimulator.Script.IsExtendedFrame, Mode=TwoWay}" Margin="0,22,0,0"/>
      </Grid>

      <!-- 状态列表 + 选中状态转移编辑 -->
      <Grid Grid.Row="4">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="110"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal">
            <Button Content="+S" Command="{Binding EcuSimulator.AddStateCommand}" Padding="4,0"/>
            <Button Content="-S" Command="{Binding EcuSimulator.RemoveStateCommand}" Padding="4,0" Margin="2,0,0,0"/>
          </StackPanel>
          <ListBox Grid.Row="1" ItemsSource="{Binding EcuSimulator.States}"
                   SelectedItem="{Binding EcuSimulator.SelectedState}" DisplayMemberPath="Name"/>
          <Button Grid.Row="2" Content="+Trans" Command="{Binding EcuSimulator.AddTransitionCommand}" Margin="0,2,0,0" Padding="4,0"/>
        </Grid>
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" Background="#CCCCCC"
                      ResizeBehavior="PreviousAndNext"/>
        <!-- 转移列表 + 编辑面板 -->
        <Grid Grid.Column="2">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="Transitions" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button Content="-Trans" Command="{Binding EcuSimulator.RemoveTransitionCommand}" Margin="6,0,0,0" Padding="4,0"/>
          </StackPanel>
          <ListBox Grid.Row="1" ItemsSource="{Binding EcuSimulator.SelectedState.Transitions}"
                   SelectedItem="{Binding EcuSimulator.SelectedTransition}">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding ServiceIdHex}" FontFamily="Consolas"/>
                  <TextBlock Text="{Binding ToState, StringFormat='  → {0}'}" Foreground="Gray" Margin="4,0,0,0"/>
                </StackPanel>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
          <!-- 转移属性编辑 -->
          <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="140">
            <StackPanel DataContext="{Binding EcuSimulator.SelectedTransition}">
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="SID:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding ServiceIdHex, UpdateSourceTrigger=PropertyChanged}" Width="52" FontFamily="Consolas" Margin="4,0,0,0"/>
                <TextBlock Text="Sub:" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBox Text="{Binding SubFunctionHex, UpdateSourceTrigger=PropertyChanged}" Width="52" FontFamily="Consolas" Margin="4,0,0,0"/>
                <TextBlock Text="Delay:" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBox Text="{Binding ResponseDelayMs, UpdateSourceTrigger=PropertyChanged}" Width="52" Margin="4,0,0,0"/>
              </StackPanel>
              <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="Mask:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding DataMaskHex, UpdateSourceTrigger=PropertyChanged}" Width="110" FontFamily="Consolas" Margin="4,0,0,0"/>
                <TextBlock Text="Pattern:" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBox Text="{Binding DataPatternHex, UpdateSourceTrigger=PropertyChanged}" Width="110" FontFamily="Consolas" Margin="4,0,0,0"/>
              </StackPanel>
              <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="ToState:" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding DataContext.EcuSimulator.States, RelativeSource={RelativeSource AncestorType=Window}}"
                          DisplayMemberPath="Name" SelectedValuePath="Name"
                          SelectedValue="{Binding ToState, Mode=TwoWay}" Width="90" Margin="4,0,0,0"/>
                <TextBlock Text="Response:" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding ResponseMode, Mode=TwoWay}" Width="70" Margin="4,0,0,0">
                  <ComboBoxItem Content="Static" Tag="{x:Static e:EcuResponseMode.Static}"/>
                  <ComboBoxItem Content="Dynamic" Tag="{x:Static e:EcuResponseMode.Dynamic}"/>
                </ComboBox>
              </StackPanel>
              <!-- 响应参数: 按 ResponseMode 显隐 -->
              <StackPanel Margin="0,4,0,0" Visibility="{Binding ResponseMode, Converter={StaticResource EcuResponseModeToVisibilityConverter}}">
                <TextBlock Text="Static data (hex):" VerticalAlignment="Center"/>
                <TextBox Text="{Binding StaticDataHex, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
              </StackPanel>
              <StackPanel Margin="0,4,0,0" Visibility="{Binding ResponseMode, Converter={StaticResource EcuResponseModeToVisibilityConverter}, ConverterParameter=Dynamic}">
                <TextBlock Text="Generator:" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding DataContext.EcuSimulator.GeneratorNames, RelativeSource={RelativeSource AncestorType=Window}}"
                          SelectedItem="{Binding GeneratorName, Mode=TwoWay}" Width="150" Margin="4,0,0,0"/>
              </StackPanel>
            </StackPanel>
          </ScrollViewer>
        </Grid>
      </Grid>

      <!-- DID 值编辑器 -->
      <StackPanel Grid.Row="5" Margin="0,6,0,2">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="DID Values" FontWeight="SemiBold" VerticalAlignment="Center"/>
          <Button Content="+DID" Command="{Binding EcuSimulator.AddDidValueCommand}" Margin="6,0,0,0" Padding="4,0"/>
          <Button Content="-DID" Command="{Binding EcuSimulator.RemoveDidValueCommand}" Margin="2,0,0,0" Padding="4,0"/>
        </StackPanel>
        <ItemsControl ItemsSource="{Binding EcuSimulator.DidValues}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal" Margin="0,1">
                <TextBox Text="{Binding KeyHex, UpdateSourceTrigger=PropertyChanged}" Width="64" FontFamily="Consolas"/>
                <TextBox Text="{Binding BytesHex, UpdateSourceTrigger=PropertyChanged}" Width="150" FontFamily="Consolas" Margin="4,0,0,0"/>
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </StackPanel>

      <!-- 只读图形预览（Task 6） -->
      <Border Grid.Row="6" BorderBrush="#DDDDDD" BorderThickness="1" Margin="0,6,0,0" DataContext="{Binding EcuSimulator.Script}">
        <controls:EcuStatePreview Data="{Binding}"/>
      </Border>
    </Grid>
  </Grid>
</Window>
