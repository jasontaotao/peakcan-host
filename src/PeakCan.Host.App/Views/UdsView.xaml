<UserControl x:Class="PeakCan.Host.App.Views.UdsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Connect" Command="{Binding ConnectCommand}" Padding="8,4" Margin="0,0,8,0" />
            <Button Content="Security Access" Command="{Binding SecurityAccessCommand}" Padding="8,4" Margin="0,0,8,0" />
            <Button Content="Tester Present" Command="{Binding TesterPresentCommand}" Padding="8,4" Margin="0,0,16,0" />

            <TextBlock Text="Session:" VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBlock Text="{Binding SessionText}" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,16,0" />

            <TextBlock Text="Security:" VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBlock Text="{Binding SecurityText}" VerticalAlignment="Center" FontWeight="Bold" />
        </StackPanel>

        <!-- Main Content -->
        <TabControl Grid.Row="1">
            <!-- DID Tab -->
            <TabItem Header="DIDs">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="DID:" VerticalAlignment="Center" Margin="0,0,4,0" />
                        <TextBox Text="{Binding DidAddress, StringFormat=0x{0:X4}}" Width="80" VerticalAlignment="Center" />
                        <Button Content="Read" Command="{Binding ReadDidCommand}" Padding="8,4" Margin="8,0,4,0" />
                        <Button Content="Write" Command="{Binding WriteDidCommand}" Padding="8,4" Margin="4,0,0,0" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Value:" VerticalAlignment="Center" Margin="0,0,4,0" />
                        <TextBox Text="{Binding DidValue, UpdateSourceTrigger=PropertyChanged}" Width="400" VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Routine Tab -->
            <TabItem Header="Routines">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Routine ID:" VerticalAlignment="Center" Margin="0,0,4,0" />
                        <TextBox Text="{Binding RoutineId, StringFormat=0x{0:X4}}" Width="80" VerticalAlignment="Center" />
                        <Button Content="Run" Command="{Binding RunRoutineCommand}" Padding="8,4" Margin="8,0,0,0" />
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding RoutineResult}" />
                </Grid>
            </TabItem>

            <!-- DTC Tab -->
            <TabItem Header="DTCs">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Read DTCs" Command="{Binding ReadDtcCommand}" Padding="8,4" Margin="0,0,8,0" />
                        <Button Content="Clear DTCs" Command="{Binding ClearDtcCommand}" Padding="8,4" />
                    </StackPanel>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding DtcList}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="DTC" Binding="{Binding Code}" Width="100" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Log Panel -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="UDS Log" FontWeight="Bold" />

            <ListBox Grid.Row="1"
                     ItemsSource="{Binding LogEntries}"
                     Background="#1E1E1E"
                     Foreground="#D4D4D4"
                     FontFamily="Consolas"
                     FontSize="12" />
        </Grid>
    </Grid>
</UserControl>
