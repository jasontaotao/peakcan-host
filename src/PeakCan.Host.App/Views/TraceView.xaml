<UserControl x:Class="PeakCan.Host.App.Views.TraceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PeakCan.Host.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:TraceViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel>
        <!-- v0.6.0: ID filter bar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,4,4,4">
            <Button Content="Clear" Command="{Binding ClearCommand}" Padding="8,2" Margin="0,0,8,0" />
            <TextBlock Text="ID filter (hex prefix):" VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Width="100" VerticalAlignment="Center" />
            <TextBlock Text="{Binding FilteredCount}" VerticalAlignment="Center" Margin="8,0,0,0" />
            <TextBlock Text=" filtered" VerticalAlignment="Center" />
            <TextBlock Text="  Total: " FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0" />
            <TextBlock Text="{Binding TotalFrameCount}" VerticalAlignment="Center" />
        </StackPanel>
        <!-- v0.9.2: highlight bar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,0,4,4">
            <TextBlock Text="Highlight (hex prefix):" VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBox Text="{Binding HighlightText, UpdateSourceTrigger=PropertyChanged}" Width="100" VerticalAlignment="Center" />
            <CheckBox Content="Errors only" IsChecked="{Binding ShowErrorsOnly}" VerticalAlignment="Center" Margin="16,0,0,0" />
        </StackPanel>
        <DataGrid x:Name="TraceGrid"
              ItemsSource="{Binding Entries}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              EnableRowVirtualization="True"
              EnableColumnVirtualization="True"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              VirtualizingPanel.ScrollUnit="Pixel"
              ScrollViewer.CanContentScroll="True"
              ScrollViewer.ScrollChanged="OnScrollChanged"
              RowHeight="20"
              AlternatingRowBackground="#F8F8F8">
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsError}" Value="True">
                        <Setter Property="Background" Value="#FFCDD2" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsFd}" Value="True">
                        <Setter Property="Background" Value="#E3F2FD" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                        <Setter Property="Background" Value="#FFFDE7" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="120" />
            <DataGridTextColumn Header="Ch" Binding="{Binding Channel}" Width="60" />
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100" />
            <DataGridTextColumn Header="Type" Binding="{Binding FrameType}" Width="50" />
            <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="50" />
            <DataGridTextColumn Header="Data" Binding="{Binding DataHex}" Width="*" />
            <DataGridTextColumn Header="Decoded" Binding="{Binding Decoded}" Width="200" />
        </DataGrid.Columns>
    </DataGrid>
    </DockPanel>
</UserControl>
