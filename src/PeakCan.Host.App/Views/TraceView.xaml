<UserControl x:Class="PeakCan.Host.App.Views.TraceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PeakCan.Host.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:TraceViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel>
        <!-- v0.6.0: ID filter bar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,4,4,4">
            <Button Content="清空" Command="{Binding ClearCommand}" Padding="8,2" Margin="0,0,4,0" />
            <Button Content="导出 CSV" Command="{Binding ExportCsvCommand}" Padding="8,2" Margin="0,0,8,0" />
            <TextBlock Text="ID 过滤 (hex):" VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Width="100" VerticalAlignment="Center" />
            <TextBlock Text="{Binding FilteredCount}" VerticalAlignment="Center" Margin="8,0,0,0" />
            <TextBlock Text=" 帧已过滤" VerticalAlignment="Center" />
            <TextBlock Text="  总数: " FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0" />
            <TextBlock Text="{Binding TotalFrameCount}" VerticalAlignment="Center" />
            <TextBlock Text=" (上限: " VerticalAlignment="Center" Margin="4,0,0,0" Foreground="{StaticResource TextSecondary}" />
            <TextBlock Text="{Binding MaxRows}" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" />
            <TextBlock Text=" 行)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" />
            <!-- Task 7 (phase 2 A-5): 通道过滤（留空=全部通道，零回归） -->
            <TextBlock Text="  通道:" VerticalAlignment="Center" Margin="8,0,4,0" />
            <ComboBox MinWidth="100"
                      ItemsSource="{Binding DataContext.ChannelConnections, RelativeSource={RelativeSource AncestorType=Window}}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Channel.Id"
                      SelectedValue="{Binding ChannelFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      ToolTip="留空=显示全部通道" />
        </StackPanel>
        <!-- v0.9.2: highlight bar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,0,4,4">
            <TextBlock Text="高亮 (hex):" VerticalAlignment="Center" Margin="0,0,4,0" />
            <TextBox Text="{Binding HighlightText, UpdateSourceTrigger=PropertyChanged}" Width="100" VerticalAlignment="Center" />
            <CheckBox Content="仅错误帧" IsChecked="{Binding ShowErrorsOnly}" VerticalAlignment="Center" Margin="16,0,0,0" />
            <CheckBox Content="暂停" IsChecked="{Binding IsPaused}" VerticalAlignment="Center" Margin="16,0,0,0" />
        </StackPanel>
        <DataGrid x:Name="TraceGrid"
              ItemsSource="{Binding Entries}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              EnableRowVirtualization="True"
              EnableColumnVirtualization="True"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              VirtualizingPanel.ScrollUnit="Pixel"
              ScrollViewer.CanContentScroll="True"
              ScrollViewer.ScrollChanged="OnScrollChanged"
              RowHeight="20"
              AlternatingRowBackground="{StaticResource RowAlternate}">
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsError}" Value="True">
                        <Setter Property="Background" Value="{StaticResource FrameBgError}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsFd}" Value="True">
                        <Setter Property="Background" Value="{StaticResource FrameBgFd}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                        <Setter Property="Background" Value="{StaticResource FrameBgHighlight}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="时间" Binding="{Binding Timestamp}" Width="120" />
            <DataGridTextColumn Header="通道" Binding="{Binding Channel}" Width="60" />
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100" />
            <DataGridTextColumn Header="类型" Binding="{Binding FrameType}" Width="50" />
            <DataGridTextColumn Header="DLC" Binding="{Binding Dlc}" Width="50" />
            <DataGridTextColumn Header="数据" Binding="{Binding DataHex}" Width="*" />
            <DataGridTextColumn Header="解码" Binding="{Binding Decoded}" Width="200" />
        </DataGrid.Columns>
    </DataGrid>
    </DockPanel>
</UserControl>
