<UserControl x:Class="PeakCan.Host.App.Views.SignalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:vm="clr-namespace:PeakCan.Host.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:SignalViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="120" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="150" />
        </Grid.RowDefinitions>

        <!-- Signal DataGrid with checkbox column for chart selection -->
        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding Latest}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  ScrollViewer.CanContentScroll="True"
                  RowHeight="20"
                  AlternatingRowBackground="#F8F8F8"
                  CellEditEnding="OnCellEditEnding">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Plot"
                    Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Width="40"
                    IsReadOnly="False" />
                <DataGridTextColumn Header="Message"   Binding="{Binding Message}"   Width="160" />
                <DataGridTextColumn Header="Signal"    Binding="{Binding Signal}"    Width="160" />
                <DataGridTextColumn Header="Raw"       Binding="{Binding Raw}"       Width="100" />
                <DataGridTextColumn Header="Physical"  Binding="{Binding Physical}"  Width="100" />
                <DataGridTextColumn Header="Value"     Binding="{Binding ValueTableName}" Width="120" />
                <DataGridTextColumn Header="Unit"      Binding="{Binding Unit}"      Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- GridSplitter between table and chart -->
        <GridSplitter Grid.Row="1" Height="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Background="#CCCCCC"
                      Visibility="{Binding HasChart, Converter={StaticResource BoolToVisConverter}}" />

        <!-- OxyPlot signal chart (hidden when no chart VM) -->
        <oxy:PlotView Grid.Row="2"
                      Model="{Binding ChartModel}"
                      Visibility="{Binding HasChart, Converter={StaticResource BoolToVisConverter}}" />
    </Grid>
</UserControl>
