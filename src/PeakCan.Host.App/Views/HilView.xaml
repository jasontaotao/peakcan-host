<UserControl x:Class="PeakCan.Host.App.Views.HilView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PeakCan.Host.App.ViewModels">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <ToggleButton IsChecked="{Binding UseHardware}" Content="Hardware" Margin="0,0,12,0" />
            <TextBox Text="{Binding HardwareChannel}" Width="80" Margin="0,0,12,0"
                     IsEnabled="{Binding UseHardware}" />
            <TextBlock Text="or" VerticalAlignment="Center" Margin="0,0,12,0" />
            <TextBox Text="{Binding TracePath}" Width="200" Margin="0,0,12,0"
                     IsEnabled="{Binding !UseHardware}" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Text="{Binding DbcPath}" Width="200" Margin="0,0,12,0" />
            <TextBox Text="{Binding SuitePath}" Width="200" Margin="0,0,12,0" />
        </StackPanel>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Run" Command="{Binding RunCommand}" Padding="24,4" Margin="0,0,12,0" />
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding Results}" AutoGenerateColumns="False"
                  IsReadOnly="True" Margin="0,12,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Case" Binding="{Binding Name}" Width="*" />
                <DataGridCheckBoxColumn Header="Passed" Binding="{Binding Passed}" Width="60" />
                <DataGridTextColumn Header="Failure Reason" Binding="{Binding FailureReason}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
