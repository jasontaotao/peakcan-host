<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\PeakCan.Host.Core\PeakCan.Host.Core.csproj" />
    <PackageReference Include="Microsoft.Data.Sqlite" />
    <PackageReference Include="CommunityToolkit.Mvvm" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <!-- System.Text.Encoding.CodePages is provided transitively (NU1510); the
         explicit reference was redundant. DbcTextReader still uses
         CodePagesEncodingProvider via that transitive dependency. -->
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="PeakCan.Host.Mobile.Core.Tests" />
  </ItemGroup>

</Project>
